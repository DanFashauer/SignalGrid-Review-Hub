---
description: Polishes a prompt for Claude Opus 4.8.
argument-hint: [the prompt you want tightened]
---

Rewrite this prompt so it works well with Claude Opus 4.8.

<prompt>
$ARGUMENTS
</prompt>

Apply what generally makes a prompt stronger for a frontier reasoning model:

- **Put the task first**, context second, output format last. Long preamble before
  the ask buries it.
- **Move constraints into the prompt, not into hope.** "Don't be verbose" is hope.
  "At most 200 words, no preamble" is a constraint.
- **Give it somewhere to put uncertainty** — an explicit instruction on what to do
  when it does not know, so it does not paper over the gap.
- **Delimit anything pasted in** with tags, so instructions inside the pasted text
  cannot be mistaken for mine.
- **Name the audience and the artifact**, not the vibe.
- **Say what a good answer looks like**, ideally with one short example of the
  shape.
- **Strip anything that only flatters the model** — role-play preambles and
  "you are a world-class expert" earn nothing and cost tokens.

Return three things:

1. **The rewritten prompt**, ready to paste.
2. **What I changed and why** — bullet per change, one line each.
3. **What I could not fix from here** — anything the prompt needs that only I know.

Do not add requirements the original did not have. Tightening is not expanding.
