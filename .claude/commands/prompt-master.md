---
description: Brain dump in, clean task spec out. Start here when the idea is still messy.
argument-hint: [your unedited brain dump]
---

Below is a raw, unedited brain dump. Turn it into a task specification I could hand
to a competent stranger who has none of the context in my head.

<brain_dump>
$ARGUMENTS
</brain_dump>

Produce exactly this, and nothing else:

**Goal** — one sentence, the outcome, not the activity. If the dump contains
several goals, say so and pick the one the rest of the dump actually serves.

**Done looks like** — concrete, checkable conditions. "Fast" is not a condition;
"a decision returns in under 50ms at p95" is. If I gave you no way to check
something, write `NOT CHECKABLE AS STATED` beside it rather than inventing a
threshold.

**Context the stranger needs** — the facts, constraints and prior decisions that
are in my head and not in the dump. Pull these from the dump where they exist.

**Explicitly out of scope** — the adjacent things this is *not*. This section
prevents more rework than any other.

**Open questions** — ONLY the ones where different answers lead to materially
different work. Rank them. If a question has an obvious default, state the default
and stop asking.

**First concrete step** — what someone would do in the next ten minutes.

Rules:
- Do not flatter the idea, and do not soften a problem with it. If the dump
  contains a contradiction, name it under Open questions as a contradiction.
- Do not add scope I did not ask for. If you think something is missing, put it in
  Open questions — never silently in Done looks like.
- Preserve my words for anything that is a decision I have already made.
