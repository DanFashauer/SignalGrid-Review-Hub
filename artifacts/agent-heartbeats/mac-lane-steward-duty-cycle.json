{
  "firedAt": "2026-08-23T08:24:28Z",
  "result": "acted: the Mac returned the hmdm-auth-path-probes result and it UNBLOCKED the headwind lane. They pinned the scheme from the decompiled war (SHA1(UPPER(hex(MD5(pw))) + compiled-in salt '5YdSYHyg2U'); the JWT endpoint wants MD5 hex, never plaintext) and retracted their own earlier 'the password is not admin' - it is 'admin', the encoding was the problem. I closed the empirical loop they were blocked from running: SHA1(UPPER(md5('admin'))+salt) equals the hash observed in the container, MATCH. run-live-lanes.sh now md5-hexes before POSTing. Acked, re-queued the first-capture request with what changed."
}
