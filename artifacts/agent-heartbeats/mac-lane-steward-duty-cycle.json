{
  "firedAt": "2026-09-17T13:27:07.842Z",
  "result": "quiet — nothing owed to the Mac and NO owner escalation. Tick firedAt 2026-09-17T13:07:32.937Z, 17.1 minutes at the reading, result quiet. NO mac/* branch new or moved since 2026-09-14 19:25. Sim-request loop passes. Manifest v78 unchanged, fingerprint 4afa60cf. Two cloud->mac messages still unread (detect.ts at 3.2d, live-evidence at ~3h); neither is re-sent, because this routine sends ONE ask and the live-evidence ask is already outstanding. THIS RECORD WAS HELD BACK ONE CYCLE ON PURPOSE, and the record should say so rather than look like a skipped fire: it was due at 13:24Z, but #790 was still in flight carrying THIS SAME single-state heartbeat file, so delivering a second batch would have conflicted on exactly the line the previous one changes and been closed as superseded — the protocol's own remedy, which delivers nothing. #790 merged at 2fe62a9b once its gating check went green and this went out immediately after. The lesson worth keeping: one batch per cycle assumes the previous batch LANDED, and when a mail PR is still open the next cycle should merge it first rather than stack onto it. THE MAC LANE'S RED CHECK IS UNDERSTOOD AND IS NOT ANY PR'S DIFF — and this supersedes what I told this lane at 10:12Z and again at 12:25Z. .github/workflows/mac-runner-auto.yml carries no actions/setup-node, so the Mac ran ambient node 24.18.0 while every other lane pins 22; node 24's bundled undici throws setTypeOfService EINVAL out of writeH1 before the load driver sees a response. #789 pins it and is PROVEN on its own run: Node.js v22.23.2, crash gone, failing fast at 6:25 instead of hanging to the 30:00 job timeout. Underneath it sits a second thing: write EPIPE on the 300-request limiter burst, which the gate could not report because it awaited with Promise.all and no catch — so a DROPPED connection, the exact condition the check 'THROTTLED, not dropped' is named for, took the harness down instead of failing that check. #791 fixes that. THE TWO ARE AN ORDERED PAIR AND THE ORDER IS MEASURED, NOT PREFERRED: a pull_request run uses the workflow from its own head, so #791's own Mac run got the unpinned lane, hit node 24 and never reached the burst — verified in its artifact. Until #789 is on mainline EVERY PR's Mac check stays red for a reason unrelated to its diff, which is worth knowing before reading any Mac result. Both are owner-gated and neither was self-merged. THE OWNER ESCALATION I MADE ABOUT THIS IS WITHDRAWN: I asked for a physical look at that machine on a firewall hypothesis, having labelled it a hypothesis and escalated on it anyway, while the answer sat in a downloadable artifact."
}
