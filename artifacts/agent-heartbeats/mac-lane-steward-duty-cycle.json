{
  "firedAt": "2026-09-14T14:49:16.385Z",
  "result": "acted — mainline 9c9b94a0. Mac tick firedAt 2026-09-14T14:36:23Z quiet (healthy). live-evidence 2026-09-12 22:56 -0400 (1.7d, under 3d). No new/moved origin/mac/* branches. FINDING for the Mac lane: check-ci-liveness.mjs returned a FALSE RED on PR #742 at 14:32:48Z — it reported the mutation sweep 'not demonstrably alive' while scheduled-verification run 34853811860 had all FOUR sweep shards green 12 minutes earlier (14:14:33-14:20:54Z), inside the 48h threshold. The CI log carries ZERO per-run diagnostic lines, and that print sits after 'if (!sweep.present) continue', so all ten inspected runs were read as having no sweep job; api() throws on non-ok status so it was not a 403/5xx — an empty payload and a dark sweep are the same value to that code. This blocks the gating check on every PR while it persists. I re-ran the failed jobs once (run 34856031864) to test transience and did NOT edit the gate — check-ci-liveness.mjs is a shared surface and a rider on an unrelated PR is the wrong place for it. If the Mac lane sees the same red, it is a real gate defect wanting its own change. Also open: #741 (OIDC invalid-config fail-closed, with a retraction commit for a false 'test:oidc registered nowhere' claim I made and reverted), #742 (four prune-script fail-opens; CodeQL alerts 176/#75 are true data flows and false vulnerabilities needing a DISMISSAL the cloud lane cannot issue - no code-scanning API here, confirmed). Owner NOT escalated."
}
