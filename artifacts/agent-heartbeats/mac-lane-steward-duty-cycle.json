{
  "firedAt": "2026-09-15T09:25:10.370Z",
  "result": "quiet on branches, and the owner is NOT escalated again this cycle — the once-per-24h escalation was spent at 08:24 (recorded in the heartbeat merged as 18758b2a) and the ask has not changed, so repeating it would only cost attention. Mac tick still firedAt 2026-09-15T04:57:20.368Z — 4.45 hours, and the root cause is established rather than suspected: actions/checkout on the Mac reports that /usr/bin/git answers \"You have not agreed to the Xcode license agreements\" and falls back to \"The repository will be downloaded using the GitHub REST API\", leaving a workspace with no .git. Owner action stands: accept the Xcode licence as root, then bash scripts/mac/install-launchd.sh --status. NEW THIS CYCLE, and it is the reason the cycle is not idle: the misreporting that cost 30 minutes of diagnosis is fixed at the root and the fix proved itself in production. scripts/check-sim-requests.mjs had two states where it needed three — `git rev-parse --is-shallow-repository` FAILS in a checkout with no git, its stdout is empty, \"\" !== \"true\", so shallow came back false and the strictest arm ran: 17 results each condemned as \"the code that produced it cannot be identified\" by the one checkout that could support no verdict at all. That is golden rule 2 inverted, unknown reaching the most confident answer instead of tightening. No repository now passes the null resolver (the file's own existing \"cannot check\" path) and the caller adds one true problem; still fatal, because an unanswerable question is not a pass. Falsified in a copy of the tree with no .git — OLD exit 1 with 17 \"does not resolve\", NEW exit 1 with 0 — and then falsified again on the Mac runner itself, which ran the PR's code on the same broken checkout and printed exactly one problem naming its own path. PR #774, owner-gated on scripts/**, preflight PASSED 320 gates, breadth PASSED 56 proofs, self-test 23 -> 25 with the two added cases labelled in the file as controls on the decline path only, because they pass against the old code too. ALSO: the 08:00 nightly Mac run never fired — the most recent event=schedule runs of mac-runner-auto.yml are 2026-09-14T14:30 and 2026-09-13T12:53 for a \"0 8 * * *\" cron, which is GitHub delaying or skipping schedules, not a Mac symptom; reported, not escalated, and it would only have failed on the same broken git anyway. STATE: mainline 18758b2a; no new or moved mac/* branch (newest is 2026-09-14 19:25 decision-path-classifier-gap, already reviewed); two lane messages unread, both addressed to mac and now both stale beyond 24h, consistent with the Mac being down and not a cloud debt; sim-request loop passes locally 17/17; manifest v78 unchanged; live-evidence 2.22 days at the 08:24 reading, under the 3-day line; four open PRs of mine (#744, #772, #774 and the pages roster line) are green on everything the cloud can verify and red only on the Mac check, which check-merge-authorization correctly refuses to merge past."
}
