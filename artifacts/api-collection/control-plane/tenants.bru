meta {
  name: Tenants
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/cp/v1/tenants
  body: none
  auth: inherit
}

docs {
  Owner-scoped on real deployments. Swap the bearer to {{ownerToken}} if the operator token is refused.
}
