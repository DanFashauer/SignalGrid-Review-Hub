meta {
  name: Edge nodes
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/cp/v1/edge-nodes
  body: none
  auth: inherit
}

params:query {
  tenant: tenant_northwind
}

docs {
  Local decision planes (edge nodes) and their sync state.
}
