meta {
  name: Managed devices - full paged read
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/deviceManagement/managedDevices
  body: none
  auth: inherit
}

docs {
  The evidence read the launch connector normalizes into DeviceManagementEvidence (posture-connector.ts:92, paged via @odata.nextLink). Read-only; the adapter maps managementState/managementAgent into the engine's normalized enums and never writes back.
}
