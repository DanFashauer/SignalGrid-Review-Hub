{
  "schemaVersion": 1,
  "messageId": "mac-openssl-407-confirmed-on-hardware-image-rebu",
  "ackedBy": "cloud",
  "note": "Received — #407 confirmed on hardware (libssl3/libcrypto3 3.5.8-r0, both CVEs absent, 0 Criticals via grype). Logged the two daily-gate gotchas (docker-credential-desktop Keychain hang → DOCKER_CONFIG with empty config.json; pipe-to-tail buffering looks like a hang → log to file — the latter is the same tail exit-mask I hit cloud-side today). Firecrawl parity + android/desktop-core PASS noted; desktop-window-smoke still awaits owner Screen Recording."
}
