{
  "schemaVersion": 1,
  "id": "2026-09-11-re-mint-evidence-manifest-v70",
  "requestedAt": "2026-09-11T16:18:31Z",
  "requestedBy": "cloud lane (DR-036 readiness lever; supersedes lane message cloud-re-mint-hardware-evidence-vs-manifest-ce58)",
  "reason": "DR-036 readiness figure, dimension (b): artifacts/live-evidence/mac-run.json carries manifestFingerprint 6f6a… while artifacts/sync/live-sync-manifest.json is at version 70 / 69899db7… (proofCounts moved after the last two runbook partials were modeled), so evidenceDimension scores 0 and the headline is 0% although (a) is 82% and (c) is 100%. Only a Mac mint can move it. This request rides the same branch as manifest v70 so it becomes runnable exactly when that branch lands on SignalGrid_Alpha — a mint against an older manifest would close the request and be stale again on merge.",
  "runs": [
    "evidence"
  ],
  "notes": [
    "Run from a fresh pull of SignalGrid_Alpha AFTER this file is on it; the manifest the evidence must match is the one in the same tree.",
    "scripts/verify-all.mjs now stamps a top-level ISO mintedAt into mac-run.json, and scripts/check-readiness-figure.mjs prefers it over the git commit date — the minted file carries its own age (FRESH_DAYS=7).",
    "Needs SIGNALGRID_MCP_PATH pointing at the sibling DanFashauer/signalgrid-mcp checkout; the op refuses on CI and refuses unless both halves are green, so a red half is a refused run, not evidence.",
    "The request is committed BEFORE the run so provenance.commit resolves in a fresh clone."
  ]
}
