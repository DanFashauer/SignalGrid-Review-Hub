# SignalGrid — solution status

**Verdict: GREEN (partial — see not-run)** · branch `land/1019` @ `820521f1` · generated by `scripts/status-summary.mjs`

> This file describes commit `820521f1`. If `git rev-parse --short HEAD` differs,
> these numbers are STALE — regenerate with `pnpm run status -- --write`. Nothing
> auto-updates it; a status file that silently ages is worse than none.

## Repo state
- vs `origin/SignalGrid_Alpha`: **6 behind / 13 ahead**
- working tree: clean
- pushed: **unknown — could not read the remote ref (offline, no remote credentials, detached HEAD, or no matching remote branch)**

## Raised hands — what is stuck, and who can clear it

```
Raised hands — 3 open (PRs NOT CHECKED here — the hourly issue job checks them)

  Needs you (Dan) — 3
    · [43.6h] landing PR #1005 (DR-043 attach rules on the live /v1 gate) — blocked by: the gitleaks secret scan flags the enum value legacy_125khz as a key; the session's classifier refuses a scanner-config edit — needs: the owner's OK to allowlist that exact word in .gitleaks.toml (or pin the two findings), then merge #1005 — https://github.com/DanFashauer/SignalGrid-Review-Hub/pull/1005 (raised by cloud; `2026-09-23-pr1005-secret-scan-false-positive`)
    · [43.6h] landing four real macOS collector fixes (XProtect, updates, screen lock, codesign) — blocked by: they live in signalgrid-mcp #14 and #15, outside the cloud lane's repository access — needs: the owner reviews and merges signalgrid-mcp #14 and #15 — DanFashauer/signalgrid-mcp #14, #15 (raised by cloud; `2026-09-23-signalgrid-mcp-14-15-merge`)
    · [43.6h] keeping the build Mac itself trustworthy — blocked by: its own posture: firewall off, not MDM-enrolled, no Time Machine, display never sleeps — needs: on the Mac: firewall on, a Time Machine disk, display sleep; enroll in Fleet when ready (raised by cloud; `2026-09-23-build-mac-hygiene`)

  Hand-raising health: 3 raised by an agent, 0 found by the system with no hand raised.
```

## Gates

| Gate | Result |
| --- | --- |
| Invariants (fail-closed / determinism / Assist / truth) | ✅ pass |
| Docs sanity (required docs + unsafe-claim scan) | ✅ pass |
| Guard-registry drift | ✅ pass |
| Docs↔proof FIGURE guard | ✅ pass |
| Proof-count sync | ✅ pass |
| Live-sync manifest (external builders) | ✅ pass |
| Preflight (typecheck, build, all proofs, browser E2E) | ⚪ not run (heavy — re-run with --full) |

## Live-sync loop (external builders: iOS EnterpriseShell + Mac MCP lane)
- manifest: version **84**, fingerprint `7c15496cfc1b936f…`
- real-hardware evidence: **fresh**

## Live-vendor lanes (opt-in — read real vendor software, not fixtures)

| Lane | Needs | Would run here now? |
| --- | --- | --- |
| `proof:live-edr` | `WAZUH_URL` — docs/ZERO_COST_LIVE_TEST_MATRIX.md §6 | no — `WAZUH_URL` unset; `verify:live` can self-provision it in Docker, which is NOT probed here |
| `proof:live-fleet` | `FLEET_URL` — docs/FLEET_LIVE_INTEGRATION.md | no — `FLEET_URL` unset; `verify:live` can self-provision it in Docker, which is NOT probed here |
| `proof:live-keycloak` | `KEYCLOAK_URL` — docs/KEYCLOAK_LIVE_INTEGRATION.md | no — `KEYCLOAK_URL` unset; `verify:live` can self-provision it in Docker, which is NOT probed here |
| `proof:live-location` | `TRACCAR_URL` — docs/TRACCAR_LIVE_INTEGRATION.md | no — `TRACCAR_URL` unset; `verify:live` can self-provision it in Docker, which is NOT probed here |

`pnpm run verify:live` stands up what Docker allows, runs those lanes and removes what it started. A lane it cannot provision is reported SKIPPED with the reason — never counted as passed. All four lanes self-provision (Wazuh since 2026-08-21, pinned 4.14.7; its first pull is ~2GB — omit it with --only fleet,location,keycloak).

> ⚠️ `proof:live-glpi`, `proof:live-headwind`, `proof:live-fleet-workflow` looks like an external lane but has no entry in this report's LANE_ENV — add one, or it goes unlisted and this section quietly under-reports.

## Truth state — what "done" means here

| State | Meaning | Where SignalGrid is |
| --- | --- | --- |
| **Implemented** | Code exists and its proof gate passes | **155** proof gates |
| **Fixture-backed** | Runs on public-safe fixtures; no live tenant, no customer data | **all connectors** |
| **Live-validated** | Read real vendor software in an opt-in lane | **4** lane(s) available; run `pnpm run verify:live` for actual results |
| **Customer-validated** | A named organisation ran it against their own estate | **none** |

Implemented is not fixture-backed is not live-validated is not
customer-validated. Nothing here has reached the fourth state, and no
document, demo or gate can move it there — only a customer can.

## Inventory
- proof gates: **155** · live-vendor lanes: **4** · browser E2E specs: **10** · CI workflows: **17**

## Not verified by this run
- Preflight (typecheck, build, all proofs, browser E2E) — heavy — re-run with --full

> Anything listed here was **not executed**, so this report makes no claim about it.

### Known hardware/licensing gates (cannot be closed in software)
- On-device MDM enforcement (kiosk / app-allowlist / non-removable) requires a **supervised** iOS device (Apple Business Manager + APNs).
- Fleet team-transfer enforcement requires **Fleet Premium**; posture read works on open-source Fleet.
