{
 "$comment": "Claims about this repository that were asserted and PROVEN FALSE. An AI contributor starts every session with no memory of the last one, so the same wrong claim can be made indefinitely. This file is that memory, made mechanical. Enforced by scripts/check-known-false-claims.mjs, which re-verifies every refutation against the tree AND fails if any tracked document re-states one. Never delete an entry to make the gate pass: if the tree genuinely changed, update the entry deliberately and say so, or the lesson is lost.",
 "schemaVersion": 1,
 "claims": [
  {
   "id": "android-absent",
   "claimed": "Android does not exist in any form — zero .kt, .gradle or android files",
   "assertedBy": "docs/DELIVERY_GAP_ANALYSIS.md, then repeated by two external analyses",
   "assertedOn": "2026-08-08",
   "whyItHappened": "The document was measured correctly and then overtaken the same morning by PR #196. Everyone downstream quoted the document instead of the tree.",
   "refutation": {
    "kind": "path_exists",
    "paths": [
     "native/android/app/src/main/kotlin/com/signalgrid/assist/MainActivity.kt",
     ".github/workflows/android.yml"
    ]
   },
   "denials": [
    "android[^.\\n]{0,40}\\babsent\\b",
    "\\bzero\\b[^.\\n]{0,30}\\.kt\\b",
    "android does not exist",
    "no android (app|files|surface)"
   ]
  },
  {
   "id": "tauri-desktop-absent",
   "claimed": "No Electron, Tauri, .csproj or .sln files exist — there is no native desktop shell",
   "assertedBy": "docs/DELIVERY_GAP_ANALYSIS.md",
   "assertedOn": "2026-08-08",
   "whyItHappened": "Same stale-document error; native/desktop landed in PR #199 hours later.",
   "refutation": {
    "kind": "path_exists",
    "paths": [
     "native/desktop/app/tauri.conf.json",
     "native/desktop/app/src/main.rs"
    ]
   },
   "denials": [
    "no electron, tauri",
    "zero electron",
    "native (windows ?/ ?macos )?desktop[^.\\n]{0,30}\\babsent\\b"
   ]
  },
  {
   "id": "dock-firmware-absent",
   "claimed": "Embedded firmware is ABSENT — zero .ino, .c, .cpp or .h files in the entire repository",
   "assertedBy": "docs/DELIVERY_GAP_ANALYSIS.md",
   "assertedOn": "2026-08-08",
   "whyItHappened": "THE SEARCH WAS THE ERROR, not the tree. It enumerated C-family extensions only, so it could not have found the firmware whether or not it existed — the firmware is Rust. This is the exact failure absence-check.mjs exists to prevent: one pattern shape is not exhaustion.",
   "refutation": {
    "kind": "path_exists",
    "paths": [
     "firmware/dock/core/src/lib.rs"
    ]
   },
   "denials": [
    "embedded firmware[^.\\n]{0,40}\\babsent\\b",
    "zero `?\\.ino`?[^.\\n]{0,60}files in the entire repository"
   ]
  },
  {
   "id": "only-one-native-platform",
   "claimed": "iOS is the only genuinely native platform",
   "assertedBy": "docs/DELIVERY_GAP_ANALYSIS.md, then repeated by two external analyses",
   "assertedOn": "2026-08-08",
   "whyItHappened": "Inherited from the three absence errors above rather than measured on its own.",
   "refutation": {
    "kind": "path_exists",
    "paths": [
     "native/android",
     "native/desktop",
     "firmware/dock/core/src/lib.rs",
     "native/ios"
    ]
   },
   "denials": [
    "one genuinely native platform",
    "only ios is (genuinely )?native"
   ]
  },
  {
   "id": "launch-profile-stranded",
   "claimed": "The launch-profile scope decision is stranded on an unmerged branch — variously '7 files versus 2' and 'the branch is 220 commits ahead' — and merging it blocks everything else",
   "assertedBy": "docs/DELIVERY_GAP_ANALYSIS.md, then two external analyses independently, as their #1 finding",
   "assertedOn": "2026-08-08",
   "whyItHappened": "PR #152 merged on 2026-08-12 (e11f2cc) and the document was never restamped. The numbers were never file counts: 7 was a commits-ahead count, 2 was a modified-file count, and 220 was invented outright — the branch was 8 ahead and 0 behind when it was written. THIS IS THE MOST EXPENSIVE ENTRY HERE: it survived two independent reviews because it was already shaped like a measurement.",
   "refutation": {
    "kind": "file_contains",
    "file": "scripts/preflight.mjs",
    "contains": "check-launch-profile.mjs",
    "paths": [
     "scripts/launch-profile.mjs",
     "docs/LAUNCH_PROFILE.md",
     "scripts/check-launch-profile.mjs",
     "scripts/src/launch-profile-proof.ts"
    ]
   },
   "denials": [
    "launch profile[^.\\n]{0,60}stranded",
    "scope decision[^.\\n]{0,40}stranded",
    "220 commits ahead"
   ]
  },
  {
   "id": "proof-gate-count",
   "claimed": "This repository has roughly 180 proof gates (elsewhere: 122)",
   "assertedBy": "two external analyses, one high and one low",
   "assertedOn": "2026-08-12",
   "whyItHappened": "Neither analysis counted. The repo publishes the real figure in docs/STATUS.md and binds it with two gates (check-status-figures, check-proof-counts); both analyses quoted a remembered number instead, in documents whose whole argument was that this repo's discipline is measured figures. NOTE 2026-08-15: this entry originally pinned the literal count (132) as its refutation evidence, and broke the first time a proof was ADDED — the count is owned by the gates, so the registry now pins the MECHANISM (a gate-bound published figure exists) rather than any value of it.",
   "refutation": {
    "kind": "file_contains",
    "file": "docs/STATUS.md",
    "contains": "proof gates: **",
    "paths": [
     "docs/STATUS.md",
     "scripts/check-proof-counts.mjs",
     "scripts/check-status-figures.mjs"
    ]
   },
   "denials": [
    "~?180 proof gates",
    "roughly 180 proof",
    "122 proof:? gates",
    "122 `?proof:\\*`? gates",
    "proof gates: \\*\\*(180|122)\\*\\*"
   ]
  },
  {
   "id": "route-owner-absent",
   "claimed": "routeOwner does not exist anywhere in the codebase today (listed among five fields that are 'a design target')",
   "assertedBy": "docs/SIGNALGRID_CLOUD_PLATFORM_AND_CYBER_RESILIENCE_ARCHITECTURE.md",
   "assertedOn": "2026-08-25",
   "whyItHappened": "A TRUE narrow claim was written as a BROAD one. The narrow claim — routeOwner is not a field on `Decision`, and the IT-layer model puts owner routing on no wire by design — is correct and still stands. It was generalised to 'does not exist anywhere in the codebase', which is false: owner routing ships as scripts/it-layer-model.mjs plus a console mirror that check-it-layer-model.mjs fails the build over in both directions. The other four fields in the same sentence genuinely appear nowhere, which is what made the fifth easy to wave through — a list is only as true as its weakest member, and nobody re-checks a member that arrived with four correct siblings.",
   "refutation": {
    "kind": "path_exists",
    "paths": [
     "artifacts/signalgrid-app/src/lib/route-owner.ts",
     "scripts/check-it-layer-model.mjs"
    ]
   },
   "denials": [
    "`?routeOwner`?[^.\\n]{0,60}(do(es)? not exist|not exist anywhere)",
    "[Ff]ive of those fields do not exist anywhere"
   ]
  },
  {
   "id": "live-vendor-proofs-run-in-ci",
   "claimed": "The live-vendor proofs (Fleet, Keycloak, FreeRADIUS/RADIUS, Wazuh) each run in CI",
   "assertedBy": "docs/EXECUTIVE_ONE_PAGER.md (proof-status section), before 2026-09-01",
   "assertedOn": "2026-09-01",
   "whyItHappened": "The proofs are committed and were each run live once on the maintainer's Mac lane; a buyer-facing summary compressed 'committed proof, proven live on the lab' into 'runs in CI', which is stronger and false — the live-server proofs hard-exit without a provisioned server and no workflow provisions one. Only the in-process live-idp proof runs in CI.",
   "refutation": {
    "kind": "file_contains",
    "file": "scripts/src/live-fleet-proof.ts",
    "contains": "process.env.FLEET_URL",
    "paths": [
     "scripts/src/live-fleet-proof.ts"
    ],
    "$why": "The live-fleet proof reads FLEET_URL from the environment and refuses without a provisioned server; no workflow in .github/workflows sets it, so it cannot run in CI. The deterministic proof:fleet-connector is a DIFFERENT proof that legitimately runs in CI — do not conflate them."
   },
   "denials": [
    "each driven by a committed proof that runs in CI",
    "(fleet|keycloak|freeradius|wazuh)[^.\\n]{0,60}\\blive[^.\\n]{0,60}\\bruns? in CI\\b"
   ]
  },
  {
   "id": "pr-366-deleted-webhooks-emitter",
   "claimed": "PR #366 (Ponytail cut 2) deleted lib/integrations/src/integrations/webhooks/emitter.ts",
   "assertedBy": "PR #366 description and its squash commit message (45cdecf), 2026-09-01",
   "assertedOn": "2026-09-01",
   "whyItHappened": "The deletion was staged in the sandbox with git rm, and the landing patch was taken with a plain git diff (index vs worktree), which cannot see a staged change; the PR text described the sandbox, not the patch.",
   "refutation": {
    "kind": "file_contains",
    "file": "docs/BUILD_BACKLOG.md",
    "contains": "went in the third cut, not the second",
    "paths": [
     "docs/BUILD_BACKLOG.md"
    ],
    "$why": "the backlog row that tracked the file records where it actually went"
   },
   "denials": [
    "emitter\\.ts \\(whole file\\)",
    "(cut 2|second cut|#366)[^.\\n]{0,60}\\b(deleted|removed)\\b[^.\\n]{0,40}emitter\\.ts"
   ]
  },
  {
   "id": "cut-4-queryclientprovider-zero-consumers",
   "claimed": "Ponytail cut 4 (commit ff27e75) removed QueryClientProvider from signalgrid-desktop and signalgrid-mobile-pwa as a provider with zero consumers (the same statement about signalgrid-review and signalgrid-web is TRUE: neither imports @workspace/api-client-react)",
   "assertedBy": "the cut-4 record in docs/BUILD_BACKLOG.md and the ff27e75 commit message, 2026-09-02",
   "assertedOn": "2026-09-02",
   "whyItHappened": "The consumer count was a grep for useQuery/useMutation over the app's own src, which is zero; every page's data hook is generated into @workspace/api-client-react on top of react-query, so the provider was the data path. The vite build stayed green because the hooks resolve without a client at build time; only the client-surfaces E2E (desktop reached the api-server: 0 calls) saw it.",
   "refutation": {
    "kind": "file_contains",
    "file": "docs/BUILD_BACKLOG.md",
    "contains": "generated into `@workspace/api-client-react` on top of react-query",
    "paths": [
     "artifacts/signalgrid-desktop/src/App.tsx",
     "artifacts/signalgrid-mobile-pwa/src/App.tsx"
    ],
    "$why": "the corrected record names the consumer; the two App.tsx files mount the provider again"
   },
   "denials": [
    "(signalgrid-desktop|signalgrid-mobile-pwa|desktop client|mobile PWA|the PWA)[^.\\n]{0,160}QueryClientProvider[^.\\n]{0,80}\\b(zero|no)[- ]consumer",
    "QueryClientProvider[^.\\n]{0,80}\\b(zero|no)[- ]consumer[^.\\n]{0,160}(signalgrid-desktop|signalgrid-mobile-pwa|desktop client|mobile PWA)"
   ],
   "$scope": "The denials name the two apps on purpose: review and web mount no api-client-react hook, so 'zero consumers' is true there and must stay sayable."
  },
  {
   "id": "security-reference-specs-not-portable",
   "claimed": "The eight `tests/security-reference/` Vitest specs are not portable onto this monorepo — they target the retired DEV Next.js server, so their invariants cannot be held on the /v1 surface",
   "assertedBy": "docs/COMPANY_BUILD_PLAN.md row 43 (\"settled what they are: not portable, and honestly labelled\") and row 71, which set the action to TRIAGE, NOT REPAIR",
   "assertedOn": "2026-08-24",
   "whyItHappened": "The ENDPOINT SHAPE was unportable and that was mistaken for the INVARIANT being unportable. /api/session/start does not exist here, but 'a repeated mutating request must not execute twice' does — as idempotency on /v1. Every spec was read for what it called, never for what it asserted, so a directory of live invariants was dispositioned as a fossil and left unexecuted for nine days.",
   "refutation": {
    "kind": "file_contains",
    "file": "docs/COMPANY_BUILD_PLAN.md",
    "contains": "the portability verdict\n    was wrong for seven of the eight specs",
    "paths": [
     "artifacts/api-server/test/api.test.mjs",
     "scripts/src/webhooks-proof.ts"
    ],
    "$why": "Seven of eight invariants were ported and are EXECUTED: `pnpm --filter @workspace/api-server run test:api` reported 363/363 at that port (was 346/346; 370/370 today — the total grows, so read it from output) and `pnpm run proof:webhooks` reports 119/119 (was 90/90). Each added assertion was falsified once against the real tree before being kept. Only fail-closed-fallbacks block 1 (`checkApiKey`/`ADMIN_API_KEY`) was genuinely unportable — neither symbol exists in this repository."
   },
   "denials": [
    "security-reference[^.\\n]{0,140}\\b(are|were) not portable\\b",
    "security-reference[^.\\n]{0,140}\\bcannot be ported\\b",
    "\\b(are|were) not portable\\b[^.\\n]{0,140}security-reference"
   ],
   "$scope": "The denials require the ASSERTIVE form naming the directory in the same sentence, so the corrected records — which say the portability verdict was wrong, and that block 1 alone was unportable — stay sayable. 'Unportable' about `checkApiKey` specifically is TRUE and must remain writable."
  },
  {
   "id": "step-up-deferred-no-shipping-surface",
   "claimed": "Step-up is a DEFERRED family, so there is no shipping surface to port a step-up test onto",
   "assertedBy": "docs/COMPANY_BUILD_PLAN.md row 43 and the DECLARED_UNEXECUTED reason string in scripts/check-test-execution.mjs, both 2026-08-23/24",
   "assertedOn": "2026-08-23",
   "whyItHappened": "'Deferred' was read off the launch profile, where step-up is correctly absent from the Limited GA fence, and then restated as 'no shipping surface exists' — a claim about the CODE. The two are different: the routes are built, mounted and covered under review-demo; they are simply not GA-claimable. The reason string then sat inside a gate, where it read as an audited decision.",
   "refutation": {
    "kind": "file_contains",
    "file": "artifacts/api-server/src/routes/v1.ts",
    "contains": "router.post(\"/v1/step-up/challenge\"",
    "paths": [
     "artifacts/api-server/src/routes/v1.ts",
     "artifacts/api-server/src/lib/profile.ts"
    ],
    "$why": "Four step-up routes are live in routes/v1.ts at :622 /v1/step-up/enroll/options, :640 /enroll/verify, :682 /challenge and :745 /v1/app-workflows/complete-step-up, with assertions beside them in test/api.test.mjs. They ship under review-demo and are NOT on the Limited GA fence: GA_ALLOWED_ROUTES in src/lib/profile.ts lists no /v1/step-up path, and src/lib/assurance.ts:52-55 derives stepUpAnswerable from exactly that. Built and claimable are different; the original claim denied the first."
   },
   "denials": [
    "\\bno shipping surface to port\\b",
    "step[- ]?up enforcement[^.\\n]{0,80}\\ba deferred family\\b",
    "\\ba deferred family\\b[^.\\n]{0,80}step[- ]?up enforcement"
   ],
   "$scope": "Scoped to the CONJUNCTION that was false — step-up deferred THEREFORE nothing exists to port a test onto. Two adjacent statements are TRUE and must stay sayable, and earlier drafts of these denials flagged both; the denials were narrowed rather than the honest sentences rewritten. (1) docs/agent/PONYTAIL_AUDIT_2026-09-01.md:96 — step-up is deferred as a GA CLAIM, which profile.ts enforces. (2) docs/COMPANY_BUILD_PLAN.md:321 — the WebAuthn step-up SESSION store (lib/webauthn/src/stepUpStore.ts) really does have no shipping surface; it has zero callers, and it is a different module from the webauthn/store.ts the live /v1/step-up routes use."
  },
  {
   "id": "enterpriseshell-talks-to-legacy-api-sessions",
   "claimed": "EnterpriseShell currently talks to an older /api/sessions/* endpoint rather than /v1",
   "assertedBy": "native/ios/SignalGridMobile/CONVERGENCE.md, 'Relationship to EnterpriseShell'",
   "assertedOn": "2026-08-30",
   "whyItHappened": "Written from memory of the shell's ORIGINAL client (task #62 found it posting to unserved /api/sessions/* paths on 2026-09-02) and never re-read after that client was rewritten to /v1 the same week. The only 'api/sessions' strings left in BackendService.swift are comments saying the server does NOT serve them, and DecisionServiceTests pins the /api/v1 path — the doc stated the opposite of the code and of the sibling test.",
   "refutation": {
    "kind": "file_contains",
    "file": "native/ios/EnterpriseShell/Services/BackendService.swift",
    "contains": "\"api/v1/sessions/start\"",
    "paths": [
     "native/ios/EnterpriseShell/Services/BackendService.swift",
     "native/ios/EnterpriseShellTests/DecisionServiceTests.swift"
    ],
    "$why": "BackendService.swift:246 builds the request against api/v1/sessions/start (refresh and end beside it); the test target asserts the /api/v1 path."
   },
   "denials": [
    "talks to an older `?/api/sessions/\\*`? endpoint rather than"
   ],
   "$scope": "Scoped to the present-tense claim about the endpoint; the convergence target that remains (one Swift client) stays sayable."
  },
  {
   "id": "verdict-core-has-no-named-reader",
   "claimed": "engine.ts, decision.ts, policy.ts, resolution.ts, evidence.ts, continuity.ts, decisionEngine.ts have no named reader at all in review-coverage.json",
   "assertedBy": ".claude/agents/verdict-core-reader.md 'Why you exist' and the verdict-core-reader charter in docs/agent/agent-tiers.json",
   "assertedOn": "2026-08-23",
   "whyItHappened": "True on the morning the role was written, discharged by that role the same day (every named file carries a verdict-core-reader entry dated 2026-08-23), and never re-read: the instruction kept dispatching a finished first read as the reason to exist, with the ledger 395 entries past the '38 files' it cited. Absent-evidence-read-as-evidence pointed at the org.",
   "refutation": {
    "kind": "file_contains",
    "file": "docs/agent/review-coverage.json",
    "contains": "lib/signalgrid-core/src/engine.ts",
    "paths": [
     "docs/agent/review-coverage.json",
     ".claude/agents/verdict-core-reader.md"
    ],
    "$why": "review-coverage.json carries reader entries for every file the claim named unread."
   },
   "denials": [
    "compute every allow/step_up/restrict/deny — have \\*\\*no",
    "exactly one of them touches the decision core"
   ],
   "$scope": "Scoped to the two sentences that stated the discharged premise as current; the role's rewritten paragraph dates the premise and is not a re-assertion."
  },
  {
   "id": "preflight-mirrors-three-of-six-ci-jobs",
   "claimed": "preflight mirrors three of six CI jobs; the three it cannot run are durable-persistence (Postgres), deploy-stack (Docker) and secret-scan (gitleaks)",
   "assertedBy": ".claude/skills/signalgrid/SKILL.md, .claude/skills/signalgrid-core/SKILL.md and .claude/skills/signalgrid-scribe/SKILL.md (the scribe line offered it as the CORRECTION to an earlier false claim)",
   "assertedOn": "2026-08-22",
   "whyItHappened": "True-ish the week it was written and retyped rather than derived. scripts/lib/ci-jobs.mjs records the same hardcoded array of three as the defect it was built to replace; check-preflight-ci-parity.mjs derives 31 jobs, 3 mirrored, 23 uncovered, and secret-scan is not in the main workflow at all. A reader who trusted the list concluded everything else was covered. CLAUDE.md fixed its own copy on 2026-09-05 with the words do not retype a number here; the skills that every role loads kept the number (twelfth audit round, 2026-09-06).",
   "refutation": {
    "kind": "file_contains",
    "file": "scripts/lib/ci-jobs.mjs",
    "contains": "MIRRORED",
    "paths": [
     "scripts/lib/ci-jobs.mjs",
     "scripts/check-preflight-ci-parity.mjs"
    ],
    "$why": "the mirrored set is DERIVED in ci-jobs.mjs and printed by the parity gate on every run; any typed count is a fossil the moment a workflow changes."
   },
   "denials": [
    "mirrors three of six CI jobs",
    "durable-persistence[^\\n]{0,40}deploy-stack[^\\n]{0,40}secret-scan",
    "six jobs and preflight mirrors three"
   ],
   "$scope": "Scoped to the typed coverage list; ci-jobs.mjs quoting the bad array as history and this registry are the two places the words legitimately remain."
  },
  {
   "id": "change-window-queued-not-built",
   "claimed": "the change-window question (are we inside the approved change window right now?) is answered by nothing in the tree; it is queued in BUILD_BACKLOG.md, not built here",
   "assertedBy": "docs/inspiration/OT_ICS_SCADA_API_CATALOG.md (section 'The one genuine near-term gap: change-window currency') and docs/inspiration/INSPIRATION.md (the OT catalog's disposition line)",
   "assertedOn": "2026-08-02",
   "whyItHappened": "True the day the catalog was compiled and never revisited: the change-window family landed on 2026-08-02 (lib/integrations/src/integrations/change-window/, proof:change-window, BUILD_BACKLOG row marked DONE) and the catalog kept describing it as a queued gap for five weeks. A reference document that states an absence is a claim with a shelf life, and nothing re-ran check:absence on it (eighteenth audit round, 2026-09-06).",
   "refutation": {
    "kind": "path_exists",
    "file": "lib/integrations/src/integrations/change-window/index.ts",
    "paths": [
     "lib/integrations/src/integrations/change-window/index.ts",
     "lib/integrations/src/integrations/change-window/types.ts",
     "lib/integrations/src/integrations/change-window/evaluate.ts"
    ],
    "$why": "the family's own source is the refutation; its types.ts defines ChangeWindowStanding = inside | outside | unknown, which is the question the catalog said nothing answered."
   },
   "denials": [
    "change[- ]window[^.\\n]{0,80}(not built here|queued in)",
    "nothing answers[^.\\n]{0,80}change window"
   ],
   "$scope": "docs/**/*.md except this registry and docs/agent/EVIDENCE.md; a sentence in the past tense that names the date it was corrected is not a denial"
  }
 ]
}
