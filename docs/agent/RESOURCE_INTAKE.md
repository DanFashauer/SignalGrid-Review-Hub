# Resource intake — what the owner feeds in, and what became of it

**Why this file exists (DR-021, 2026-08-31).** The owner feeds resources
continuously — repositories, articles, tools, vendor material — and directed
that they be absorbed: *"I'm going to be feeding you information all the time
for you to absorb and use to better strengthen the repo."* Every resource gets
a row here: what it was, what was done with it, and what it changed in the
tree. A row saying "evaluated, not adopted" must name what was actually run or
read — not just reasons.

**The failure mode this replaces, recorded so it is not repeated.** On
2026-08-30 the owner shared the ECC toolkit and asked for it to be set up and
used. The first response was a memo of cautions with a question at the end —
correct facts, wrong posture. The owner's correction the next day ("absorb and
use") is the operating rule now: a resource is answered with a working
integration, a measured result, or a named blocker — never with only reasons.

## Intake log

| Date | Resource | Directed by | Disposition | What it changed |
| --- | --- | --- | --- | --- |
| 2026-09-12 | An unattributed listicle the owner pasted, "How to Install and Use Claude Code Plugins for Efficient AI Coding" — Ponytail ("up to 75% fewer tokens", `!ponytail ultra`, "6 efficiency questions"), OmniRoute ("~1.6B free tokens/month", "300+ free providers"), Graphify ("70x", `!graphify index`), "Agent Skills" ("24 pre-built skills", `!skills load …`), installed with `!install ponytail, omni_route, graphify, agent_skills` | Owner: *"I want this with all the other tools and resources I've provided already to you now does this make sense???"* | **Evaluated by five independent read-only reads and refuted on its mechanics; three of the four tools are real, two were already absorbed, one is new.** Every command in it is fabricated: in Claude Code `!` is shell mode, so each would be handed to zsh as a missing binary; the real forms are `/plugin marketplace add owner/repo` + `/plugin install name@marketplace`, `/ponytail [lite\|full\|ultra\|off]`, `/graphify .`, `/<skill-name>` (claude-code-docs + the local CLI 2.1.269; none of the four names is in the 295-plugin official marketplace). Ponytail: real (DietrichGebert/ponytail, MIT) and ALREADY ABSORBED — row 2026-09-01, DR-024, `pnpm run ponytail:install`; its README says −22% tokens / ~54% less code / −20% cost, a seven-rung ladder, modes `lite\|full\|ultra` — no "75%", no "6 questions", no "light". OmniRoute: real and ALREADY ABSORBED by reference (DR-029, `docs/AGENT_GATEWAY.md`, row 2026-09-04); upstream says 352 providers / 56 keyless / ~1.51B tokens re-audited biweekly, "no keys, no config" — the article's "2+ API keys" and "1.6B" match neither upstream nor the repo. "Agent Skills": not a product — the agentskills.io standard this repo's 26 tracked skills already follow (DR-030); no "24 pre-built skills" pack exists (`check:absence` CORROBORATED, 0 of 295 marketplace plugins). Graphify: real and NEW — Graphify-Labs/graphify (Apache-2.0, PyPI `graphifyy` 0.9.58, a CLI + skill + PreToolUse hook, not a marketplace plugin); its own table shows 71.5x on ONE 52-file mixed corpus and ~1x on a small pure-code library, so "70x" is one row of one benchmark. | This row. Two findings surfaced by the check, not the article: (1) `pnpm run ponytail:install` FAILS on this Mac — the pinned commit 2ed6c52 ("= v4.9.0") is not fetchable from upstream and is not among the 60 commits below the real v4.9.0 tag (0a4dd63a, 2026-08-08), so the vetted pin cannot be reproduced; mailed to the cloud lane (which vetted it) — not re-pinned here, a re-vet is a review, not a Mac edit. **[Cloud, same day, refuted: the object exists — 3 commits after tag v4.9.0 (tests + a Grok Build adapter; the skills tree is byte-identical to the tag; its manifest reads 4.9.0). The installer fetched a 7-character id, which `git fetch` reads as a REF NAME, not an object — fixed by pinning the full id in `scripts/install-ponytail.mjs`; the DR-024 vetting stands. Evidence entry 2026-09-12.]** (2) The Graphify trial (`uv tool install graphifyy` in a throwaway worktree) was refused by the Mac's auto-mode permission classifier; it waits on the owner running that one command, after which the Mac measures it on this tree before anything is wired in (it writes into `.claude/skills/`, `CLAUDE.md`, `settings.json` and an untracked `graphify-out/` that would flip sim-result provenance unless ignored). |
| 2026-09-12 | [Graphify-Labs/graphify](https://github.com/Graphify-Labs/graphify) (`safishamsi/graphify` redirects there) — codebase knowledge graphs for Claude Code: Python, Apache-2.0, PyPI `graphifyy` 0.9.58, pinned at `23f2ffaa43fd12f25d9eabe91e6d184b5d89b474` (default branch `v8`, 2026-09-10) | Owner: the listicle in the row above ("cuts token usage by 70x"); the Mac could not trial it (its classifier refused the install), the cloud measured it in a sandbox | **Evaluated by USE — not adopted; offline research lens at most.** Run in a sandbox at the pin against a COPY of `lib/` + `scripts/` (956 indexed files), no keys, no install into any session: `extract --code-only` exit 0 in 23.67 s, 11,489 nodes · 18,176 edges · 553 communities; the same pipeline with every socket denied by a shim produced the identical graph, so graph-building needs no network and no LLM (`Token cost: 0 input · 0 output`). What kept it out: (1) its shipped Claude Code integration registers PreToolUse hooks that run its binary before EVERY Bash/Grep/Read/Glob call, and in strict mode DENIES the agent's first raw file read — a third-party binary auto-executed from a hook is exactly what this repo forbids; (2) its git post-commit hook rebuilds into `graphify-out/` inside the checkout, and `provenance.workingTreeClean` counts untracked files, so every later sim result would read as minted from a dirty tree; (3) rebuilds over an existing output directory oscillate (five `--force` runs of an unchanged tree alternated 11,489 / 11,464 nodes, dropping connector and concept nodes), so its answer is not a pure function of the source; (4) the "70x" figure is not in its README (docs claim 71.5x on a mixed corpus, unreproduced); its own `benchmark` printed `463.4x` for our corpus against a denominator of nodes × 50 words because the real word count is written to one path and read from another — the real count is 2.2× larger; `graph.json` itself is 11.6 MB, LARGER than the 10.4 MB of source it describes, and two of three sample queries anchored on the wrong files. Honest, useful, and cheap: cold builds are byte-reproducible, and `GRAPH_REPORT.md` (102 KB) lists god nodes, import cycles (none in lib/+scripts) and surprising connections no current gate prints. If ever used: a pinned copy run by hand in a scratch directory, never `install`, never a hook, never `graphify-out/` inside the checkout, nothing it emits cited by a gate, a proof, a doc figure or the decision path. Nothing of it is in the tree. | `docs/agent/RESOURCE_INTAKE.md` |
| 2026-08-31 | IAM core-architecture framework diagram (authentication / identity / authorization boundaries around an access decision engine; no author attribution on the graphic) | Owner: shared under absorption mode | **Absorbed.** A six-reader parallel sweep held all 92 of its concepts against the tree's real code, proofs and gates: 83 proven with citations that `check:cited-paths` now verifies on every run, 3 doctrine-only, 3 absent-relevant, 3 out-of-scope with the boundary stated. Its Access Decision Engine box independently mirrors the Decision Envelope + DR-020 cascade. Product: [`docs/research/IAM_CORE_COVERAGE_MAP.md`](../research/IAM_CORE_COVERAGE_MAP.md) + three backlog rows (credential-revocation route+proof, the iOS shell's dangling SAML config keys, the undesigned retention/deletion admin job). | `docs/research/IAM_CORE_COVERAGE_MAP.md`, `docs/BUILD_BACKLOG.md`, `docs/INDEX.md` |
| 2026-08-31 | API-types taxonomy infographic ("The Complete Guide to API Types," Sathish Kumar Subramani) — open / internal / partner APIs classified by audience | Owner: shared as pattern input under absorption mode | **Absorbed.** Evaluated against the repo's real API surface: the docs classified the three served surfaces by mechanism but never by audience. `docs/API_ACCESS_AND_CONNECTORS.md` now carries a "Who each surface is for" table — `/v1` partner-facing (full contract promises), `/cp/v1` internal (fewer promises on purpose, not for third-party integration), the demo surface explicitly NOT an open API. The load-bearing sentence the taxonomy surfaced: a route being reachable does not make it supported. | `docs/API_ACCESS_AND_CONNECTORS.md` |
| 2026-08-30 | [affaan-m/ECC](https://github.com/affaan-m/ECC) — agent-harness toolkit (`ecc-universal`, MIT): review/plan/build-fix passes for coding agents | Owner: "use this as an overall strategy and final pass or additional passes" | **Absorbed.** The Mac lane hosts the harness dormant, report-only, hooks off. Its first pass (2026-08-31) found four verified fail-closed inversions in the #336 self-check tooling; all four fixed, three further findings verified and fixed, the suite wired into preflight. Cloud sessions install on demand: `pnpm run ecc:install` (pinned `ecc-universal@2.2.0`, user scope, hooks off, non-interactive — verified by a live install in the cloud sandbox 2026-08-31 on the owner's go: payload is 68 agents / 286 skills / 94 commands / 23 rules at git `005eff4`, hooks shipped but disabled, the repo's own `.claude/settings.json` untouched; commands load at next session start, so ephemeral containers re-run the script per session). One installer side effect to know: it writes `includeCoAuthoredBy: false` into user settings. ECC advises; only preflight/verify:breadth certify green (DR-021 §4). | `scripts/classify-failure.mjs`, `scripts/check-gate-census.mjs`, `scripts/scan-gaps.mjs`, `scripts/preflight.mjs`, `scripts/verify-breadth.mjs`, `docs/agent/KNOWN_CONDITIONS.json`, `package.json` (`ecc:install`) |
| 2026-09-01 | "Modern Enterprise Cybersecurity Architecture Stack" reference diagram — a five-layer industry model (AI/agent governance, data/DSPM, zero-trust identity mesh, cloud-workload, secure-network/SASE); no author attribution on the graphic | Owner: shared under absorption mode | **Absorbed.** Mapped the five layers against the tree's real `proof:*` gates and connector families (140 proofs / ~48 deferred families read from `scripts/launch-profile.mjs` and `package.json`). Finding: SignalGrid maps cleanly as the stack's **decision layer** — its weight sits in Layer 3 (Zero Trust Identity Mesh, the launch surface) and it reaches the other four layers as read-only, fail-closed *evidence sources*, not as a replacement for any of them (SASE / CWPP / KSPM / DSPM / DLP-enforcement stay out of scope, host/platform responsibility). The map independently re-derives the DR-020 thesis: SignalGrid fuses signals from the whole stack, and the worker never sees it. Every row cites a proof that exists today; no new claim, no new surface. Product: [`docs/research/ENTERPRISE_SECURITY_STACK_COVERAGE_MAP.md`](../research/ENTERPRISE_SECURITY_STACK_COVERAGE_MAP.md). | `docs/research/ENTERPRISE_SECURITY_STACK_COVERAGE_MAP.md` |
| 2026-09-01 | Firecrawl marketing email (Alex Nucci, VP Revenue) — CLI + Agent Skill + hosted MCP server, "turn websites into LLM-ready data"; advertised install `npx -y firecrawl-cli@latest init --all --browser` | Owner: *"Please also add this on top of ECC please."* | **Absorbed — adopted as an opt-in research/verification lane (DR-022), superseding the prior "not needed" call in `docs/INTAKE_LEDGER.md` row 97.** The owner's direction overrides row 97; recorded rather than silently reversed. Added `pnpm run firecrawl:install` (`scripts/install-firecrawl.mjs`): registers the **MIT** `firecrawl-mcp@3.24.0` hosted-API client as a **user-scoped** MCP server for Claude Code — the opposite of the vendor one-liner on every axis (pinned not `@latest`, one client not `--all`, fail-CLOSED on a missing key not `-y`). The API key lives in `FIRECRAWL_API_KEY` (env), never the repo. AGPL concern sidestepped: the hosted API via the MIT client, not the self-hosted AGPL-3.0 server. Report-only — never a decision path, proof fixture, connector, or the product build. Owner-only step: supply the key via env once. | `scripts/install-firecrawl.mjs`, `package.json` (`firecrawl:install`), `docs/DECISION_RECORDS.md` (DR-022), `docs/INTAKE_LEDGER.md` (row 97 annotated), `.claude/skills/signalgrid-evidence-toolchain/SKILL.md` |
| 2026-09-01 | [DietrichGebert/ponytail](https://github.com/DietrichGebert/ponytail) — "Lazy senior dev mode for AI agents" (MIT, v4.9.0 @ 2ed6c52; 6 skills: ponytail / -review / -audit / -debt / -gain / -help; 3 harness hooks; a read-only MCP that serves the ruleset) | Owner: "This will now be the primary layer that runs and controls this repo … top; then ECC; then my own build scanned by both; then your independent scan; then converge and execute. I'm hands off." | **Absorbed — adopted as the TOP layer of the review stack (DR-024).** Evaluated by USE: cloned, manifest validated, installed user-scope pinned to the local clone (never the moving HEAD), default mode set to `ultra`. Hooks vetted clean (text injection only) and installed, unlike ECC's hooks-off — reason recorded. What it IS: a minimalism lens — the seven-rung ladder (YAGNI → reuse → stdlib → native → installed dep → one line → minimum) with hard "never cut" rules for trust-boundary validation, data-loss handling, security, accessibility, and one runnable check per non-trivial logic. What it is NOT: a correctness/security scanner (its own audit skill says so) — that is why ECC and the fail-closed/contract/runtime scans sit beneath it. First independent scan (`ponytail-audit`, ultra, three parallel auditors: lib / artifacts / scripts+native) run 2026-09-01; `ponytail-debt` baseline: clean ledger. | `scripts/install-ponytail.mjs`, `package.json` (`ponytail:install`), `docs/DECISION_RECORDS.md` (DR-024), `docs/agent/PONYTAIL_AUDIT_2026-09-01.md` |
| 2026-09-01 | [nhadaututtheky/neural-memory](https://github.com/nhadaututtheky/neural-memory) — persistent memory for AI agents (MIT, v4.62.0 @ 2015cb9b): a Python MCP server (`nmem-mcp`) over a local SQLite graph, recall by spreading activation; also ships a plugin form (PyPI-latest server + four Claude Code hooks) | Owner: *"This need to be added to the stack this is essential that this is added."* | **Absorbed — adopted as the MEMORY SUBSTRATE under the DR-024 stack (DR-026), not a review layer: it remembers, it judges nothing.** Evaluated by USE: cloned, read, installed with `uv` pinned to the commit, registered as a user-scoped MCP server, `claude mcp get` shows it connected. Plugin form REFUSED: PyPI-latest server plus four hooks, two of which ingest the Claude Code transcript and one of which logs every tool call — hooks OFF, as for ECC. Config written once (never overwritten) turns off the PyPI version check, Mem0, sync and Telegram. Store at `NEURALMEMORY_DIR` OUTSIDE the repo (installer refuses an in-tree path), `.neuralmemory/` gitignored because its surface writer targets any `.git`/`package.json` root. Operating memory only — never tenant data, secrets, PHI, live-evidence, or an index of the tree; committed docs stay the memory of record; nothing in `lib/*`, `artifacts/api-server` or a proof may touch it. Upstream defect noted (concurrent synapse adds on one SQLite connection drop edges); the owner filed it upstream on 2026-09-02 from the cloud lane's draft (the lane cannot post to a repository outside its scope). | `scripts/install-neural-memory.mjs`, `package.json` (`neural-memory:install`), `.gitignore` (`.neuralmemory/`), `docs/DECISION_RECORDS.md` (DR-026), `.claude/skills/signalgrid-evidence-toolchain/SKILL.md` |
| 2026-09-02 | "MCP vs Skills" infographic (Level Up Coding, blog.levelupcoding.com; image shared by the owner, not committed — third-party artwork stays outside the publication boundary). Its split: MCP = a client/server protocol that exposes an external system's capabilities to a model; a skill = an instruction file the model selects by metadata that shapes the workflow, output format and constraints. | Owner: shared the image with no instruction; absorbed under the standing intake rule. | **Absorbed — used to state, in the architecture doc, how this repository already uses each and why the two are not interchangeable here.** Evaluated by use: `artifacts/mcp-server/` is the repo's MCP server (read-only tools over the fixture-backed decision core; DR-008: an orchestration interface, not a trust authority) and `.claude/skills/` is the skill registry (25 tracked directories: 14 vendored obra/superpowers skills plus 11 first-party role and ritual skills, per `.claude/skills/VENDORED.md`; the count is derived, not remembered — `git ls-files .claude/skills | awk -F/ 'NF>3{print $3}' | sort -u | wc -l`). Nothing in the infographic changes a decision; what was missing was one paragraph saying which instrument carries which responsibility, so a reader does not reach for a skill to expose a capability or for an MCP tool to encode a rule of conduct. | `docs/MCP_ARCHITECTURE.md` (Plane 2: "MCP and skills are different instruments") |
| 2026-09-02 | [public-apis/public-apis](https://github.com/public-apis/public-apis) — community-curated directory of public APIs (MIT, `38527bc`): one README of 1721 link rows under 51 categories with contributor-supplied Auth / HTTPS / CORS columns; no endpoint definitions, limits or terms | Owner: *"Here are additional resources and layers that's can be added or provide more information and detailed answers about my product as well."* | **Absorbed — as an evidence/research catalogue beside Firecrawl (DR-027), not a review layer, not a memory layer, not a connector.** Evaluated by USE: cloned at depth 1, categories and rows counted from the clone, and ten read-only GET probes with synthetic inputs (a well-known CVE id, `1.1.1.1`, a public landmark coordinate, a year+country, a generic UA string) — NVD CVE 2.0, MSRC CVRF, Nager.Date, Nominatim and four IP-to-ASN/geo services answered 200 keyless; **URLhaus, marked `No` auth, returned 401** (the directory's auth column drifts); ApicAgent's row links a landing page; four IP-geo sources disagreed on the city for one anycast address. Ranked sixteen rows against the real families: NVD → `vuln-scan` (CVE fixture enrichment), Nager.Date / gov.uk → `change-window` and `shift-context` (holiday tables), Nominatim → `location-services` (a facility geofence centre at design time, ODbL) are the fits; IP reputation, HIBP and MAC-vendor rows are key-gated or out of the product's lane; OSV, CIRCL, KEV, EPSS, macvendors.com and time-zone services are NOT in the directory at this sha. Rule added: fixture-first — a public API's output enters the tree only as a committed, dated fixture; live only behind tier + `SIGNALGRID_LIVE_INTEGRATIONS` + a configured opt-in and the ungated-fetch gate; a closed list of non-identifying inputs; outage → `unknown` → raise. Supersedes `docs/INTAKE_LEDGER.md` row 97's "wrong category" for the narrow enrichment question; row 97 stands for systems of record. No code, no dependency, no family, no proof. | `docs/research/PUBLIC_API_SOURCES.md`, `docs/DECISION_RECORDS.md` (DR-027), `docs/INTAKE_LEDGER.md` (row 97 annotated), `docs/INDEX.md` (a deferred family, not shipping) |
| 2026-09-02 | "4 main types of data centers" infographic (Excellog.Biz; image shared by the owner, not committed — third-party artwork stays outside the publication boundary): enterprise/private, colocation, hyperscale/cloud-provider, edge, each with typical uses and a primary value (control and governance, flexibility and interconnection, scale and efficiency, proximity and response) | Owner: *"Might want to also consider the size and scaling that matters too."* | **Absorbed.** The taxonomy became the physical layer under the two-plane split the repo already states: a hosting-tier table in [`docs/DEPLOYMENT_MODELS.md`](../DEPLOYMENT_MODELS.md) mapping each tier to the plane it hosts, why, and what happens when the link between planes drops — with the WAN-partition and edge-appliance claims marked as design targets no proof runs. The owner's sizing point became a demand model (events per source, sources from the site's own inventory, one worked example labelled assumption) set against the three dated measured figures in `RELIABILITY_SLO.md` in the order they bind: the shipped 240/min limiter first, the HTTP path second, the core last; and a stated list of what does not scale by adding nodes yet (one ledger chain, per-process limiter, no multi-node plane run). By use: two prose sections and one backlog row; no fixture, no gate, no proof, no code, no new measurement. The independent review found the one artifact this should have produced and did not — a figure-provenance binding for the performance numbers quoted outside `RELIABILITY_SLO.md` — and it is filed as a backlog row rather than claimed. | `docs/DEPLOYMENT_MODELS.md`, `docs/BUILD_BACKLOG.md` |
| 2026-09-02 | MCP Market ([app.mcpmarket.com](https://app.mcpmarket.com)) — a public directory of Model Context Protocol servers; the owner's creator page `app.mcpmarket.com/dan-fashauer/mcp/new` is authentication-walled and could not be enumerated | Owner: search MCP Market for anything that adds value to SignalGrid, *"so I can offer it as a solution."* | **Absorbed — as a fixture-first ecosystem map plus a public-safe listing for SignalGrid's own MCP server (DR-028); not a connector, not a dependency.** Evaluated by USE: read the public MCP directories and vendor documentation on 2026-09-02 (the auth-walled deploy/creator side was not enumerated) and mapped the ecosystem to the externally-sourced connector families derived from `lib/integrations/src/integrations/` — Okta/Auth0/Keycloak/Entra/Casdoor for identity, CrowdStrike/SentinelOne/Defender/Tanium/Velociraptor for EDR, Splunk/Sentinel/Elastic for SIEM, ServiceNow/Jira/PagerDuty for ITSM, Meraki Magic for NAC, Seam for physical access, Fleet/Jamf/Intune/osquery for UEM, Trivy/Snyk for vulnerability scanning, Grafana/Datadog/Honeycomb for observability; the three categories with no widely-adopted MCP server (DLP, peripheral control, carrier) are named as explicit gaps rather than omitted. The listing describes the existing read-only MCP server (`artifacts/mcp-server/`) drift-gated by `scripts/check-mcp-surface.mjs`, for the owner to paste into his creator page — only he can publish it, he has the login. A machine gate (`scripts/check-mcp-ecosystem-map.mjs`, preflight + CI, self-tested both directions) holds the map to the real family set. No live external MCP server is wired; no code, no dependency, no family, no partnership or certification claim. | `docs/research/MCP_ECOSYSTEM_SIGNAL_SOURCES.md`, `docs/SIGNALGRID_MCP_MARKET_LISTING.md`, `docs/DECISION_RECORDS.md` (DR-028), `scripts/check-mcp-ecosystem-map.mjs`, `scripts/preflight.mjs`, `.github/workflows/review-hub-ci.yml`, `docs/INDEX.md`, `package.json` (`check:mcp-ecosystem-map`) |
| 2026-09-03 | Two MCP Market leaderboards shared by the owner (screenshots, not committed — third-party artwork stays outside the publication boundary): "Top Agent Skills" (82 entries) and "Top 100 MCP Servers" (top ~82 by GitHub stars) | Owner: *"use ones that will add extra skills and value to building and completing this business"* | **Absorbed by use.** Every beneficial entry held against the tree in a disposition-by-use catalogue. Most were ALREADY COVERED and recorded as such rather than rebuilt (Context7, Playwright, GitHub, Firecrawl → connected/installers; Codebase-Memory/Graphiti/Cognee/Claude-Context/Serena/Beads → Neural Memory DR-026; FastMCP/Mcp-Go/MCP-Agent/Eino → the repo's own `artifacts/mcp-server/package.json`; the orchestration/prompt/repo-scan/security/TDD/worktree skills → named repo skills and agents). Three genuine SAFE non-duplicative gaps were built repo-native (no external MCP install, no duplicate skill): a skill/agent-plane conformance gate `scripts/check-skill-plane-conformance.mjs` (self-tested, wired into preflight + CI — fills the gap between the roster gates, neither of which checked SKILL.md front-matter shape); the `research-ops` skill (evidence-first market/competitive/discovery research, `check:absence`-confirmed absent, serving the #1 discovery blocker); and a one-command dev-MCP lane setup `scripts/setup-mcp-lane.mjs` (`pnpm run mcp:setup`) + `scripts/install-context7.mjs` that makes the already-adopted servers reachable on either lane, keys env-only. Out-of-scope categories (memes, social, music, trading, 3D/Blender, bioinformatics, telephony, reverse-engineering, Vue/Kotlin/Django/Go stacks, OS/VM control, LOB verticals, vertical clinical skills) named as categories with the boundary stated. Folded in the owner's lane-parity addendum: skills/agents/`signalgrid-mcp` already travel through git to both lanes; only the dev MCP servers install per machine. | `docs/research/MCP_MARKET_LEADERBOARDS.md`, `docs/MCP_AND_SKILLS_LANE_PARITY.md`, `.claude/skills/research-ops/SKILL.md`, `scripts/check-skill-plane-conformance.mjs`, `scripts/setup-mcp-lane.mjs`, `scripts/install-context7.mjs`, `scripts/publication-boundary.mjs`, `.claude/skills/VENDORED.md`, `docs/MCP_ARCHITECTURE.md`, `scripts/preflight.mjs`, `.github/workflows/review-hub-ci.yml`, `package.json`, `mac-kickoff.sh`, `docs/INDEX.md` |
| 2026-09-04 | [diegosouzapw/OmniRoute](https://github.com/diegosouzapw/OmniRoute) — self-hosted AI gateway (MIT): one OpenAI-compatible endpoint in front of ~352 providers / 1200+ models, with routing strategies, free-tier token aggregation, token compression, three-layer resilience, and its own MCP server (110 tools) + A2A; Node 18+/Docker/Bun, optional Redis; requires upstream provider keys/OAuth | Owner: *"added as part of the stack for tools to building and completing the project … used across all lanes and all layers within the repo and org."* | **Absorbed — adopted as the org's AGENT/BUILD AI-gateway (the model-access layer for the coding lanes), by reference and keys-out-of-tree (DR-029); NOT a product component.** Evaluated by USE: read the repo's README + feature surface on 2026-09-04 — by its own purpose ("never stop coding": provider fallback + free-tier aggregation) it is build/agent infrastructure, the model-ACCESS layer a coding lane runs on, not a signal source, connector, or decision component. Adopted by reference (self-hosted, MIT) — not vendored, not a package dependency, so its 352-provider surface stays out of this repo's supply chain. BOUNDARY (load-bearing, golden rule 2): it may never enter the decision path — nothing in `lib/*`, `artifacts/api-server`'s `/v1` path, a connector or a proof may call it; a model decides no verdict. Keys are env-only owner secrets. Runtime adoption across lanes is owner infra (self-host + provision provider keys; a lane's model endpoint is set by its environment, not the repo), so the repo ratifies and documents the adoption but cannot provision it. No code, no dependency, no family, no proof, no gate, no claim. | `docs/AGENT_GATEWAY.md`, `docs/DECISION_RECORDS.md` (DR-029), `docs/INDEX.md` |

| 2026-09-04 | Operational runbooks for a real large-hospital shared-clinical-device deployment (L1 + L2 support guides, iOS device-prep, iOS update, macOS setup) — the "why" behind the product | Owner: *"the reason why I'm creating this product … used for reference and motivation … so this should allow you to make more sense of it."* | **Absorbed — the domain ground truth mapped onto the tree, not a tool to install.** The five runbooks describe the multi-vendor mesh SignalGrid abstracts: badge-based MAM check-out of shared iPhones from charging docks, a UEM for posture, Apple supervision-identity "device trust," and a network-tethered internet-sharing layer every workflow depends on. Absorbed by USE: the workflow and its failure taxonomy were held against the repository's real surfaces — [`docs/research/SHARED_DEVICE_CUSTODY_GROUND_TRUTH.md`](../research/SHARED_DEVICE_CUSTODY_GROUND_TRUTH.md) maps each element to the event contract (`lib/event-contract`), connector families (`custody-beacon`, `rtls-custody`, `pacs-access`, `passkey-assurance`, `device-attestation`, `nac`, `carrier`, `local-authority`, `break-glass`, `change-window`, `macos-posture`, `ddm-connector`), the cross-domain detections (`detect.ts`), and the remediation-allow cascade — dense with **modeled** rows (the design fits the domain almost one-to-one) and **three named fidelity gaps** filed to the backlog as **design targets** (not shipped): a "phantom custody / stale-or-contested" detection, the checkout-cap contradiction, and a faithful end-to-end smart-charging scenario — each DR-020 territory, deferred and proposed rather than changed here. The two premises that run through every page — nearly every failure root-causes to network/pairing, and remediation today is manual tribal knowledge — are exactly golden rule 2 and the deterministic-playbook thesis. **Publication boundary held hard:** the confidential source PDFs are NOT committed, and the map carries no customer, personnel, purchase-order, site, internal-URL, directory-group or network-address specifics. | `docs/research/SHARED_DEVICE_CUSTODY_GROUND_TRUTH.md`, `docs/BUILD_BACKLOG.md`, `docs/INDEX.md` |

| 2026-09-04 | Four reference-architecture infographics (images shared, not committed — third-party artwork stays outside the publication boundary): (1) an enterprise/AI security stack (Zero-Trust IdP → API gateway → AI gateway → DLP/guardrails + authorization → AI agent → RAG/cloud/IAM, with observability and conditional-access panels); (2) Hospital Information Systems (HIS) — core modules, a four-layer architecture, HL7/FHIR/DICOM standards, and 21 named HIS/EHR vendors; (3) a Check Point NGFW enterprise network architecture (perimeter HA cluster, core/distribution switching, DMZ, VLAN segmentation, site-to-site VPN); (4) a SAML-vs-OAuth flow comparison | Owner: *"more insight and references to use and extract data for this product … making sense of what I'm building."* (absorption mode) | **Absorbed by use — disposition by reference; three already covered, one new.** (1) **Security stack:** SignalGrid maps as the **decision layer**, already held against the tree in [`ENTERPRISE_SECURITY_STACK_COVERAGE_MAP.md`](../research/ENTERPRISE_SECURITY_STACK_COVERAGE_MAP.md) and [`IAM_CORE_COVERAGE_MAP.md`](../research/IAM_CORE_COVERAGE_MAP.md); the AI-gateway / agent / prompt-shielding layer is covered by DR-029 (agent gateway — a model decides no verdict) and DR-008 (the read-only MCP plane is orchestration, not a trust authority). Recorded, not rebuilt. (2) **HIS/EHR:** the genuinely new piece — added an HIS/EHR row to the ecosystem-positioning matrix ([`docs/ECOSYSTEM_POSITIONING.md`](../ECOSYSTEM_POSITIONING.md)) naming the clinical system of record (Epic, Oracle Health, …) SignalGrid sits alongside but is **not**: it reads nothing from the chart and writes nothing to it, deciding before the worker reaches the host clinical app (golden rule 3), tying to the Smart Charging host apps in [`SHARED_DEVICE_CUSTODY_GROUND_TRUTH.md`](../research/SHARED_DEVICE_CUSTODY_GROUND_TRUTH.md). (3) **NGFW network:** the firewall / VLAN / 802.1x / DMZ layer is a signal source consumed fail-closed by the `nac` / `network-nac` families, and the perimeter/edge split matches [`DEPLOYMENT_MODELS.md`](../DEPLOYMENT_MODELS.md); SignalGrid is not a firewall. Recorded, not rebuilt. (4) **SAML vs OAuth:** SignalGrid implements the **OIDC/OAuth + JWT** path ([`lib/enterprise-auth`](../../lib/enterprise-auth), reviewed fail-closed this session, #421); SAML is the adjacent host-IdP path, already tracked as the iOS shell's dangling SAML-config gap. Publication boundary held: the images are not committed and no customer or vendor-relationship claim is made. | `docs/ECOSYSTEM_POSITIONING.md` |

| 2026-09-04 | [Fechin/reference](https://github.com/Fechin/reference) — the "Quick Reference" cheatsheet site (215 sheets: bash/sed/awk/find/grep/curl/netstat/nc, git + GitHub Actions + gh + Conventional Commits, Docker/Postgres/Redis/nginx/OpenSSL, Homebrew/cron/ssh/tmux, Swift/Kotlin/Rust, TypeScript/JavaScript/npm/regex/YAML/TOML/JSON, HTTP status codes/MIME/ports/markdown, and the Claude Code/Codex/Cursor/OpenCode CLIs) | Owner: *"scan this site for any information you need to help you and add more information for our skills and org."* | **Absorbed by use — as the org's CORRECTED quick reference, not as a copy.** Nine parallel readers (one per domain group, 355 tool uses) held every relevant sheet against this tree's gates, hooks and doctrine and RAN the doubtful commands on the Mac lane; each group's output was verified before it was kept. The product is the new first-party skill `.claude/skills/stack-reference/` — `SKILL.md` distils five cross-cutting laws (two userlands one script; banned and ask-first; the names are different here; fail-closed in every language; deterministic and the tree is the provenance) and eight domain files record **102 places the generic advice would break a rule here**, each with the verified failure and the form to use instead: bash-4-only syntax that stock bash 3.2 aborts on or, worse, silently mis-runs (`{5..50..5}` loops once with garbage, exit 0); GNU sed/find/grep/awk forms BSD rejects or silently matches nothing on (`\s`, `{,m}`, `IGNORECASE`) and the reverse traps that pass here and fail on ubuntu CI (`-mtime +1w`); the hook-banned `git reset --hard` / `git stash`; npm for pnpm; `main` for `SignalGrid_Alpha` (a copied `if:` that never fires); `--expose` for `-p`; short image names; `as`-casts, `!!`, Elvis defaults, `default:`/`_` arms and `any` that fail open; 403 where the API answers 404; PCRE syntax in Node gates — plus the ~140 sheet items that survived contact (portable probe/sed/grep forms, the repo's status-code and port maps, the canonical Actions preamble, launchd for cron, the Kotlin/Rust invocations that actually run here). Twelfth first-party exception in the vendored skills directory: `VENDORED.md` count and table and the section-E carve-out updated together, skill-plane conformance green. No dependency, no external call, no claim about what ships. | `.claude/skills/stack-reference/` (`SKILL.md` + `shell.md`, `git-ci.md`, `containers.md`, `mac-host.md`, `native.md`, `typescript.md`, `ai-cli.md`, `http-docs.md`), `.claude/skills/VENDORED.md`, `scripts/publication-boundary.mjs` |
| 2026-09-06 | Fleet server configuration reference (fleetdm.com docs page, one PDF page shared by the owner alongside the Fleet Premium trial licence key — the key itself is a credential and lives OUT of tree, in a session scratch file, never committed) | Owner: shared with the trial key so the lane could bring up a Premium-licensed Fleet | USED the same day: the licence goes to the SERVER as `FLEET_LICENSE_KEY` (the reference page excerpt covered the MySQL/Redis/TLS options; the licence option was confirmed against the running server, which reported `license.tier=premium` on `GET /api/v1/fleet/config`). Cloud lab stood up, Premium surfaces measured, adapter bug found and fixed, proof section added — `docs/FLEET_LIVE_INTEGRATION.md` "Cloud-lane run, 2026-09-06". Trial expires 2026-09-16. | `docs/FLEET_LIVE_INTEGRATION.md` |
| 2026-09-07 | Claude Code **plugins reference** + the docs-index fetch note (`https://code.claude.com/docs/llms.txt`), pasted by the owner | Owner: *"add this and make this the operative of this project"* → clarified to **package the repo as a plugin** | **Absorbed by USE — packaged this repository's operating plane as a Claude Code plugin (DR-030), verified with the real CLI.** Read the reference 2026-09-07 and built against it: `.claude-plugin/plugin.json` names the plugin `signalgrid` and declares the live plane by path — `skills: ./.claude/skills/` (28), the 13 tracked `agents` files, `commands: ./.claude/commands/` (9). `claude plugin validate .` exits 0 (CLI 2.1.263; one benign CLAUDE.md-at-root warning). Two constraints surfaced by USE and recorded: `agents` rejects a directory (must be a file list), and a component path may not escape the plugin root — so the manifest sits at the repo root pointing at the real `.claude/` (one source of truth, no drift copy), and a bare manifest does not auto-activate, so current sessions are unchanged. Hooks are deliberately EXCLUDED (they stay in `.claude/settings.json`; re-declaring would double-fire and they are repo-specific). The hand-listed `agents` array — the drift class this repo hunts — is gated: `scripts/check-plugin-manifest.mjs` (preflight + CI, self-tested, mutation-proven) fails unless the list equals `git ls-files .claude/agents/*.md` and validate passes. No `lib/*`/`/v1`/connector/proof/decision-path change; no claim moves. The full vendor text is not fossilized; a pointer-and-provenance doc records the source and the facts built to. | `.claude-plugin/plugin.json`, `scripts/check-plugin-manifest.mjs`, `docs/DECISION_RECORDS.md` (DR-030), `docs/reference/CLAUDE_CODE_PLUGINS.md`, `scripts/preflight.mjs`, `.github/workflows/review-hub-ci.yml`, `docs/INDEX.md` |
| 2026-09-08 | [`mvanhorn/last30days-skill`](https://github.com/mvanhorn/last30days-skill) — `last30days` v3.23.0 (MIT), an agent-led research skill that fans a topic across 15+ recent-discussion sources (Reddit, X, YouTube, TikTok, Hacker News, Polymarket, GitHub, arXiv, Techmeme, LinkedIn, Bluesky, StockTwits, Perplexity, open-web search) and synthesizes one dated last-30-days brief ranked by live engagement | Owner: *"Need to incorporate this repo into the layers and tech stack for this project. This will help a lot I believe based on information I've provided in the past and future."* | **Absorbed by USE at the doctrine/routing level — adopted as the research-ops lane's recency transport (DR-031), the Firecrawl (DR-022) / public-apis (DR-027) slot; documentation-first, nothing installed.** Read via the published repo on 2026-09-08 (its `SKILL.md`, `CONFIGURATION.md`, `CONCEPTS.md` — not just the README) and deliberately **not** run: a live invocation transmits the query to 15+ third parties and can read local browser cookies, so it trips the "ask before anything that sends data to an external service" boundary and live use is owner-gated. Evaluated against the tree by use: it answers "what changed about X in the last ~30 days," which is the research/source-discovery job Firecrawl already owns — its output feeds the competitive briefs (`docs/research/COMPETITIVE_*.md`, `MARKET_LANDSCAPE.md`) and discovery-conversation prep, written under research-ops discipline. Boundary held hard: non-deterministic and clock/network-dependent, so it is the inverse of the deterministic core and its brief can never be a fixture, a verdict, a connector input, or a decision-path read (golden rule 2); a thin/degraded brief reads as *unknown*, not "nothing happening"; nothing identifying leaves the tree; and adopting it asserts nothing about what ships. No code, dependency, MCP install, new skill or gate — the public-apis shape, not the Firecrawl install. | `docs/research/LAST30DAYS_RESEARCH_TOOL.md`, `docs/DECISION_RECORDS.md` (DR-031), `.claude/skills/signalgrid-evidence-toolchain/SKILL.md`, `docs/INDEX.md` |
| 2026-09-08 | [`obra/superpowers`](https://github.com/obra/superpowers) — the Claude Code software-development skills framework by Jesse Vincent (14 skills: brainstorming, TDD, systematic-debugging, subagent-driven-development, using-git-worktrees, writing-skills, verification-before-completion, …) | Owner: *"Need to incorporate this repo into the layers and tech stack for this project. This will help a lot I believe based on information I've provided in the past and future."* | **Already fully vendored — verified current and byte-identical the same day; nothing new to incorporate.** This repository was vendored on 2026-08-20 at commit `b36e0829` (Release v6.3.0) into `.claude/skills/` — 14 upstream skills, 51 files + LICENSE, MIT, governed by [`.claude/skills/VENDORED.md`](../../.claude/skills/VENDORED.md) (provenance, the Overrides table, and `scripts/check-skill-instruction-conflicts.mjs`), packaged into the operating plane by DR-030 and classified by `scripts/check-publication-boundary.mjs` section E. Evaluated by USE on the re-share: the raw CDN is reachable (the API is still 403 through the proxy, which is what blocked the 2026-09-06 read from doing this), so every vendored file was re-fetched from upstream at the pin and compared — all 52 byte-identical — and the commits feed showed upstream HEAD is STILL `b36e0829`, so the pin is current and there is no newer skill to adopt (and re-vendoring the same bytes, or authoring a duplicate of a skill already here, is exactly what the "adopt only genuine safe gaps, no duplicate skills" rule forbids). The genuine strengthening: closed the open byte-identity caveat the 2026-09-06 read had to leave, by re-fetching every vendored file from the raw CDN at the pin and diffing it (fail-closed). A committed verifier script was written and then removed: it made the tree's first outbound-`fetch()` script and tripped CodeQL's file-data-in-outbound-request query, so the record is the evidence and the re-check stays a manual fetch-and-diff — not worth new network surface for a convenience. No re-vendor, no new skill, no dependency, no claim moved; no DR (nothing in green-certification changed). | `.claude/skills/VENDORED.md`, `docs/agent/RESOURCE_INTAKE.md` |
| 2026-09-09 | Owner-shared infographic — *"9 Architectures for Multi-Agent Systems"* (QuantumEdgeX): Event-Driven, Hierarchical, Decentralized, Role-Based, Collaborative Swarm, Hybrid, Centralized Orchestrator, Blackboard System, Pipeline — shared as an image alongside *"I'm looking for a 🧠 to operate things and each one of these repos I've shared will do the same thing in this graph… can use the Mac for resources/testing/even docker."* | Owner: shared as the operating-architecture pattern for the two-lane org, then directed (2026-09-09) a standing recurring cycle, full autonomous tier, daily, *"auto but does x amount of security and code check validation … for the winner."* | **Absorbed by USE — built as the Standing Brain Cycle (DR-032).** A 13-agent design workflow mapped the nine patterns onto the repo and chose Blackboard (shared audit space) + Collaborative-Swarm fan-out (the lens panel) + Centralized Orchestrator (deterministic decide) + a Pipeline gauntlet, with Role-Based routing — a Hybrid. Slice 1 ships the machinery DORMANT (`status: awaiting-activation`): a brain-freshness STEP-0 gate, the deterministic decision core, the orchestrator spine, config and the registry row, all self-tested and wired into preflight + CI; the account trigger and activation await an explicit owner go. The image itself stays outside the tree (third-party artwork, publication boundary). | `scripts/check-brain-freshness.mjs`, `scripts/brain-cycle-decide.mjs`, `scripts/brain-cycle.mjs`, `docs/agent/brain-cycle-config.json`, `docs/agent/scheduled-routines.json`, `docs/agent/BRAIN_CYCLE_DESIGN.md`, `docs/DECISION_RECORDS.md` (DR-032) |
| 2026-09-11 | The **NIST GitHub organization** ([`github.com/usnistgov`](https://github.com/usnistgov)) — U.S. government open-source security corpus: the macOS Security Compliance Project (`macos_security`), OSCAL + `oscal-content`, SP 800-63 (Rev 4, `800-63-4`), the Mobile Threat Catalogue, `dioptra` (AI eval), and the ACVP/entropy crypto-validation projects | Owner: *"an excellent one to have and use for both lanes and the whole system. Plus I'm sure this will make the security team and others happy having these resources."* | **Absorbed by use — an alignment map holding SignalGrid's real surfaces against the NIST works that carry external authority for them.** The four highest-value fits were verified against upstream (mSCP covers SP 800-53r5 / 800-171 / CIS / STIG and implements SP 800-219; OSCAL defines machine-readable assessment-results; SP 800-63 grades authenticator assurance; NVD is the national CVE feed) and mapped to what this tree actually proves: **mSCP → Mac-lane posture** (`proof:macos-posture`, `proof:macos-apple-schema`, `proof:ddm-connector` — the `macos-posture`/`uem` families are **deferred** and `lib/ddm-connector` is a workspace library the profile does not classify, `device-management-health` is the one launch family; the live posture read is the **separate `DanFashauer/signalgrid-mcp` Python server**, not the in-repo decision-fabric MCP — mSCP names which posture facts matter, SignalGrid would read them fail-closed); **SP 800-207 Zero Trust → the decision fabric** (a **subordinate SP 800-207 mapping** of the decision fabric, not the company thesis (canonical thesis: `docs/PURPOSE.md`, DR-020): `proof:zero-trust-principles` and `proof:signalgrid-core` exercise the Policy Engine's `evaluatePolicy` directly; the launch loop is **per-call/point-in-time**, continuous reevaluation is deferred and not established, and the Policy-Administrator cascade is fixture/sim (deferred)); **SP 800-63 (Rev 4) → the assurance ladder** (only the `step_up` verdict is launch; the `passkey-assurance` and `token-binding` families and the `/v1/step-up/*` routes are **deferred** — proofs are not a surface the profile classifies, all of them run in CI, and `webauthn-verify` is a proof, not a family); **NVD (CVEs) → `vuln-scan`** (deferred; extends intake-ledger row 30, fixture-first per DR-027); **SCAP/XCCDF/OVAL → the `security_baseline` signal via the deferred `benchmark-selection` family** (`docs/SECURITY_BASELINE_ALIGNMENT.md`; SignalGrid does not run a SCAP scan) — two distinct lanes, not one; **Mobile Threat Catalogue → the custody threat model** (a review lens over `SHARED_DEVICE_CUSTODY_GROUND_TRUTH.md`). Two OSCAL/mSCP items are filed as **design targets** (an mSCP-derived posture fixture; an OSCAL assessment-results export) — proposed, not built, so no claim of an exporter that does not exist. **Honesty boundary held hard:** framed only as *"aligned with / informed by,"* NEVER NIST-certified / -endorsed / a compliance attestation (NIST endorses no products; `docs/PUBLIC_MESSAGING_GUARDRAILS.md`), and `dioptra` + the crypto-validation projects are named adjacent/out-of-scope with the boundary stated. No `lib/*`, `/v1`, connector, proof, or decision-path change; no claim moved. | [`docs/research/NIST_ALIGNMENT_MAP.md`](../research/NIST_ALIGNMENT_MAP.md), `docs/INDEX.md`, `docs/INTAKE_LEDGER.md` (row 99) |

## Rules for a new row

1. Log the resource the day it arrives, before evaluating it.
2. Evaluate by USE — install it, run it, point it at this repo — not by
   reading its README and predicting.
3. Third-party code is pinned to an exact version and never auto-executed
   from a hook; adoption of a tool that runs in sessions gets a decision
   record if it changes how green is certified.
4. Security boundaries hold: no secrets into third-party tools, no tenant
   data, and the publication boundary applies to anything a tool writes into
   the tree.
| 2026-09-10 | The owner's Google Drive "Project › SignalGrid" folder, read directly by the Mac lane after the owner's correction that his inputs are *"research information from what I do for my everyday job"*, not customer engagement, and that the Drive is *"the data source of what I've had to deal with and ideas for building this solution"* | Owner: directed, in his own words (DR-034) | **Absorbed by pointer and audit — nothing from the Drive committed.** Every item matched against the tree: the five operational runbooks → `docs/research/SHARED_DEVICE_CUSTODY_GROUND_TRUTH.md` (already absorbed 2026-09-04); the invention disclosure → `docs/research/IP_AND_LICENSING.md` (confidential pointer; content never reproduced); the Ecosystem Master Catalog → `docs/INTAKE_LEDGER.md` row 28 / `docs/inspiration/TECHNOLOGY_ECOSYSTEM_MASTER_CATALOG.md`; the three API-catalog bundles → `docs/inspiration/CONTROLUP_DEX_EUC_API_CATALOG.md` and siblings; the un-logged remainder → ledger row 98. Two of roughly sixty saved screenshots sampled (a software-asset-management process diagram; a job-interview tips card): third-party reference infographics of mixed relevance, absorbed case-by-case as prior rows already do. Employer-identifying material generalized exactly as the ground-truth map does. | DR-034; `docs/INTAKE_LEDGER.md` row 98; provenance line in `docs/research/SHARED_DEVICE_CUSTODY_GROUND_TRUTH.md`; `.claude/skills/research-ops/SKILL.md` reconciled to DR-033/034 |
| 2026-09-12 | Two short videos shared by the owner (not committed — third-party footage stays outside the publication boundary; transcribed locally with an open-source speech model, transcripts kept in the session scratchpad). (a) 85 s: a Netflix infrastructure leader on why *"we need more systems thinkers in a world with AI"* — local teams once built their own stacks; with agents operating across many systems and wanting source-of-truth data, *"common infrastructure, common paved paths, solving problems once with a core set of capabilities becomes more important,"* so they hire people who abstract across business domains into building blocks. (b) 71 s: a practitioner's Claude Code workflow — brain-dump the task list, have the strongest model act as ORCHESTRATOR that writes the specs and reviews, hand execution to cheaper Opus sub-agents working in parallel worktrees, loop *"fix X, Y and Z"* until it is good enough, then report to the human. | Owner: *"Hopefully you can transcribe and use this towards my company and product."* | **Absorbed by USE, both.** (a) is buyer-side evidence for `docs/company/ICP_EVIDENCE.md` Finding 2 (standardising a plural stack is somebody's funded job): the person who owns the paved path is the person SignalGrid's orchestration layer (DR-035) is built for, and "paved path" is now the vocabulary of that finding — added as a dated supporting paragraph, with its limitation (one speaker, one company, an infrastructure org rather than a hospital IT shop) stated first. (b) is the cloud lane's own operating pattern from this date: the session model writes the spec and reviews; execution fans out to Opus sub-agents in worktrees; the loop closes only on a check that fails without the fix — recorded in `docs/LANE_COORDINATION.md` ("How the cloud lane runs build work"), and applied first to the mutation guard's pending brace-less families (BUILD_BACKLOG campaign row). Nothing from either clip is quoted as a claim about SignalGrid; no code, dependency or launch surface changed. | `docs/company/ICP_EVIDENCE.md` (Finding 2), `docs/LANE_COORDINATION.md`, `docs/agent/RESOURCE_INTAKE.md` |
| 2026-09-12 | [bradautomates/claude-video](https://github.com/bradautomates/claude-video) — the `/watch` skill ("give Claude the ability to watch any video"): Python 3 stdlib + `ffmpeg`/`ffprobe`/`yt-dlp`, scene-aware frame extraction with 16×16 grayscale dedup, transcript from publisher captions or a Whisper API call. MIT. Pinned at `83da59fa78c3eee9e20f515fe75c438bb5166efd` (`main`, v0.2.0, last commit 2026-06-30; 17,106 stars, 175 open issues on 2026-09-12) | Owner: the five-tool image ("These also need to be added and absorbed into the 🧠"), then, when the evaluation recommended against it: *"I'm going to challenge you on not adding CLI anything and the Claude video that's very essential"* | **Adopted by vendoring, owner-directed (DR-038) — the evaluation's findings stand and shaped the form.** Measured first, in a sandbox at the pin with no key, `HOME` redirected, a pinned static ffmpeg 7.0.2 on `PATH`, against one of the owner's own clips (70.61 s, 576×1024, no caption track): `--detail balanced` exit 0 in 4.53 s with 58 frames and `Transcript: none available`; `--detail transcript` returned nothing in 0.073 s; its own suite `5 failed, 66 passed` (all five: `yt-dlp` absent). The same file through the path this repo already used for the owner's videos (ffmpeg → faster-whisper `small`, CPU int8, offline) gave 1,806 characters of timestamped transcript in 63.4 s. Its transcript path is an upload — the only two HTTPS endpoints in the code are Groq's and OpenAI's `/audio/transcriptions`; it ships a `SessionStart` hook; the key-free half that works is a well-made `ffmpeg` wrapper (auto-fps, frame budget, 1998 px clamp for `Read`). Honest where it matters (its security section names both endpoints and the `$CWD/.env` fallback read); two reporting defects ("Zero config" holds only on macOS; on scene-detector fallback the report prints "58 selected from 4 candidates"). So: `skills/watch/` vendored unmodified (12 files + LICENSE) as `.claude/skills/watch/`, the hook, tests and manifests NOT taken, two instructions overridden in `VENDORED.md` rather than edited, and a first-party `video-intake/` skill that runs `/watch` for frames and `.claude/skills/video-intake/scripts/transcribe-local.py` for a key-free local transcript, then files the intake row. A Whisper key is the owner's per-machine decision, never in the tree (DR-029). | `.claude/skills/watch/` (vendored), `.claude/skills/video-intake/` (first-party), `.claude/skills/VENDORED.md` (second upstream section, two table rows, two override rows), `scripts/publication-boundary.mjs` (figure 15, two carve-outs), DR-038 |
| 2026-09-12 | [HKUDS/CLI-Anything](https://github.com/HKUDS/CLI-Anything) — "Making ALL Software Agent-Native": a Claude Code plugin whose `/cli-anything` command is a 747-line SOP for reversing a GUI app into a Python/Click CLI, 79 hand-written harnesses, 71 `skills/` dirs, and the pip package `cli-anything-hub` 0.4.1 (registry + installer + telemetry). Apache-2.0 at the root (`cli-hub/setup.py` says MIT). Pinned at `810c18b0d1ab9b234bc996c9fd999318523a3ef0` (`main`, 2026-08-21); 49,282 stars | Owner: the five-tool image, then the same challenge as the row above | **Adopted by vendoring the method, owner-directed (DR-038); the hub stays out.** Measured first, in a sandbox at the pin (venv, `HOME` redirected, nothing global): the plugin's own tests `41 passed in 0.15s`; its one deterministic tool, `skill_generator.py` (an `ast` reader of a Python/Click package), drafted a SKILL.md for the sample mermaid harness in 0.048 s and rejected a SignalGrid script outright (`ValueError: cli_anything directory not found`) — this repo has 2 `.py` files and no Python packaging, so the tool has no substrate here and the METHOD is what transfers. The hub breaks two house rules, measured: it fetches an unversioned registry from a live GitHub Pages URL and runs each entry's `install_cmd` (77 of 78 `pip install git+…` entries resolve to `main` at that moment; two carry shell metacharacters and run with `shell=True`, one of them a pipe from `curl` into `bash`); telemetry is on by default — `cli-hub --version` alone posted two PostHog events fingerprinting the running agent, and `cli-hub can <query>` posted the query string verbatim (captured on a local sink; `CLI_HUB_NO_ANALYTICS=1` silences it). The README's aggregate test figure does not reconcile (2,461 claimed; its own breakdown sums to 2,330; the per-harness table to 3,424); 2 of 5 sampled `pip install cli-anything-<name>` lines name packages PyPI returns 404 for. The plugin directory itself registers no hooks, no MCP server, no network call. So: `cli-anything-plugin/` (26 files: `HARNESS.md`, 5 commands, 8 guides, 3 stdlib Python modules, template, tests, Apache-2.0 licence) vendored unmodified under `third_party/cli-anything/`, activated by a first-party `.claude/skills/cli-anything/` skill that maps the seven phases onto the `signalgrid` CLI over `/v1` and the MCP server (TypeScript, read-only by default, fixture-tested; the build is a `docs/BUILD_BACKLOG.md` item). NOT taken: `cli-hub`, the 79 harnesses, the 71 skills, the marketplace manifests. Never `pip install cli-anything-hub` on either lane. | `third_party/cli-anything/` (vendored, own `VENDORED.md`), `.claude/skills/cli-anything/` (first-party), `scripts/publication-boundary.mjs` (area + carve-out), `docs/BUILD_BACKLOG.md`, DR-038 |
