{
  "$comment": "The employment record for every agent the harness can dispatch (.claude/agents/*.md). DR-016. Tier and write capability are DERIVED from each agent's own `tools:` frontmatter by scripts/check-agent-roster.mjs, not trusted from this file — an agent that lists Write or Edit can write, whatever it says here. What this file supplies is the part that cannot be derived: the write BOUNDARY, which must be disjoint from every other writing agent's. Vendored agents are byte-identical to third_party/everything-claude-code/agents/ and are never edited; their governance lives here instead, which is how a third-party definition gets a first-party boundary without modifying it.",
  "version": 1,
  "tiers": {
    "0": "Doctrine. DR-015's accuracy rules. Binding on every agent; owns no surface.",
    "1": "Owning roles (Core, Native, Reviewer, Scribe) defined in docs/agent/ORG.md. Broad write scope, human-sequenced.",
    "2": "Narrow specialists. One declared, disjoint write surface each.",
    "3": "Read-only reviewers and evaluators. No write tools, so they cannot collide with anyone."
  },
  "agents": [
    {
      "id": "architect",
      "tier": 3,
      "provenance": "vendored",
      "writeScope": null,
      "charter": "System design, scalability and technical decision-making. Advises; writes nothing.",
      "carvedOutOf": null
    },
    {
      "id": "planner",
      "tier": 3,
      "provenance": "vendored",
      "writeScope": null,
      "charter": "Decomposes work into ordered plans. Advises; writes nothing.",
      "carvedOutOf": null
    },
    {
      "id": "code-reviewer",
      "tier": 3,
      "provenance": "vendored",
      "writeScope": null,
      "charter": "Adversarial correctness review. Reports findings; fixes nothing, per the Reviewer boundary in ORG.md.",
      "carvedOutOf": null
    },
    {
      "id": "security-reviewer",
      "tier": 3,
      "provenance": "vendored",
      "writeScope": null,
      "charter": "Auth seams, injection, secret handling, supply chain. Reports only. LOCAL CORRECTION (2026-09-05), vendored body must NOT be edited: `npx eslint . --plugin security` and `eslint-plugin-security` are not installed; `npm audit` is `pnpm audit` here. The repo's own scanners are the ones to run: `pnpm run review:invariants`, `node scripts/check-nan-fail-open.mjs`, gitleaks/CodeQL in CI (SECURITY.md).",
      "carvedOutOf": null
    },
    {
      "id": "doc-updater",
      "tier": 2,
      "provenance": "vendored",
      "writeScope": "docs/",
      "charter": "Keeps prose matching what shipped. Bounded to docs/ so it can never edit the code its claims describe. LOCAL CORRECTION (2026-09-02) to the vendored definition, which is byte-identical to third_party/everything-claude-code/agents/doc-updater.md and must NOT be edited: its \"Analysis Commands\" block runs `npx tsx scripts/codemaps/generate.ts`, and that path does not exist in this repository — there is no codemap generator and no docs/CODEMAPS tree. Ignore that line; build a codemap by reading the tree and write it. The neighbouring `npx madge` and `npx jsdoc2md` are external packages npx fetches, not workspace dependencies, so both need network. The real in-repo generators are `node scripts/generate-sync-manifest.mjs` (never hand-edit its output) and `node scripts/gen-claim-inventory-md.mjs` (`--check` fails on drift).",
      "carvedOutOf": null
    },
    {
      "id": "e2e-runner",
      "tier": 2,
      "provenance": "vendored",
      "writeScope": "scripts/src/e2e/",
      "charter": "Drives and repairs browser end-to-end specs, and nothing outside that suite. LOCAL CORRECTION (2026-09-02) to the vendored definition, which is byte-identical to third_party/everything-claude-code/agents/e2e-runner.md and must NOT be edited: its Playwright fallback block runs `npx playwright test tests/auth.spec.ts`, and this repository has no tests/ specs at all. The suite is scripts/src/e2e/*.spec.ts (10 files). Run all of it with `pnpm run test:e2e`; run one file with `pnpm --filter @workspace/scripts run test:e2e src/e2e/review-console.spec.ts` — the spec path is relative to scripts/, because that is the package the script runs in. Verified 2026-09-02: that command with --list enumerates 10 tests in review-console.spec.ts.",
      "carvedOutOf": "scripts/"
    },
    {
      "id": "tdd-guide",
      "tier": 2,
      "provenance": "vendored",
      "writeScope": "tests/",
      "charter": "Writes failing tests before implementation. Bounded to tests/ so it cannot make an assertion pass by changing the code under test. LOCAL CORRECTION (2026-09-05) to the vendored definition, which must NOT be edited: its `npm test` / `npm run test:coverage` do not exist in this repository (root package.json has neither), and tests/ holds three k6 load scripts, not a test suite — this repository's tests are the proof harnesses under scripts/src, owned by gate-and-proof-engineer. Dispatch this role to DESIGN the failing assertion (name, input, expected refusal) and hand it to gate-and-proof-engineer to land; it must not be dispatched expecting a green `npm test`.",
      "carvedOutOf": null
    },
    {
      "id": "refactor-cleaner",
      "tier": 2,
      "provenance": "vendored",
      "writeScope": "lib/",
      "charter": "Behaviour-preserving cleanup of the decision fabric only. LOCAL CORRECTION (2026-09-05), vendored body must NOT be edited: `npx knip`, `npx depcheck`, `npx ts-prune` and `npx eslint` are not installed and must not be added ad hoc — dead-code evidence here comes from `pnpm run typecheck`, the reachability gate in scripts/ (`check-package-reachability.mjs`), and `git grep` for consumers.",
      "carvedOutOf": null
    },
    {
      "id": "build-error-resolver",
      "tier": 2,
      "provenance": "vendored",
      "writeScope": "artifacts/",
      "charter": "Resolves build and type failures in the server and web artifacts. LOCAL CORRECTION (2026-09-05), vendored body must NOT be edited: `npx eslint`, `npm run build`, `npm install` and the `rm -rf .next node_modules/.cache` recovery do not apply here — there is no ESLint config, no Next.js, installs are pnpm-only (preinstall refuses npm), and the repo's own PreToolUse hook DENIES that recovery line. Use `pnpm run typecheck`, `pnpm run build` (linux-x64 only) and `pnpm install --frozen-lockfile`.",
      "carvedOutOf": null
    },
    {
      "id": "gate-and-proof-engineer",
      "tier": 2,
      "provenance": "first-party",
      "writeScope": "scripts/",
      "carvedOutOf": null,
      "charter": "Owns every gate and proof harness in scripts/ (the largest unowned surface in the tree; count it with `git ls-files scripts | wc -l`, never from a charter). Falsifies both directions before claiming a gate works, self-tests every gate, derives scope instead of hand-listing it, and registers in preflight AND CI. Hired 2026-08-23 by the DR-016 loop: most open build-plan rows live in scripts/ and no Tier-2 agent could write there."
    },
    {
      "id": "verdict-core-reader",
      "tier": 3,
      "provenance": "first-party",
      "writeScope": null,
      "charter": "Reads the decision core that computes every verdict — first read completed 2026-08-23 and recorded in review-coverage.json; the role now re-reads what changed since each entry's date and whatever the ledger marks UNREAD or PARTIAL — and reports: dead computed fields, unexercised branches, unfailable assertions, and any path where unknown evidence could loosen rather than tighten the answer. Reports only, never fixes. Hired 2026-08-23 by the DR-016 loop to close build-plan row 42.",
      "carvedOutOf": null
    },
    {
      "id": "fail-closed-auditor",
      "tier": 3,
      "provenance": "first-party",
      "writeScope": null,
      "charter": "Audits ONE property estate-wide: when the code does not know, does the answer tighten or loosen? Hunts fail-open inversions (NaN comparisons, guards that short-circuit on unknown, empty collections concluding no-objection, absent evidence read as good evidence, catch arms minting their own success) and the same defect on a slower clock — figures, cited paths and exemptions that have drifted from what they describe. Hired 2026-08-23 after seven instances of one NaN fail-open were found live on the authentication path, none of which any existing reviewer's lens asked about. Reports with a reproduction and the SHAPE of the gate that would hold the finding; gate-and-proof-engineer builds it. Writes nothing, so it collides with no one.",
      "carvedOutOf": null
    },
    {
      "id": "agent-platform-steward",
      "tier": 2,
      "provenance": "first-party",
      "writeScope": ".claude/",
      "carvedOutOf": null,
      "charter": "The org's own executable definition: the skills, agents and commands every other role runs through. WRITES only .claude/. Its review surface is wider than its write surface — .agents/, artifacts/mcp-server/, CLAUDE.md and AGENTS.md are READ and REPORTED, never written, because artifacts/ already belongs to build-error-resolver and the two instruction files are read by every role at once."
    }
  ]
}
