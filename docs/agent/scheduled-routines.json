{
 "$comment": "The committed registry of ALWAYS-ON agent routines (role-lens review, ai-ops next-work). Every recurring scheduled lane is declared here with its cadence, the human who authorized it, what it may write, where it stops and escalates, and where its firing evidence lands. scripts/check-scheduled-routines.mjs holds this file against the heartbeat artifacts and the org roster; its header states honestly that it gates the DECLARATION and the EVIDENCE OF FIRING — it cannot read the live account scheduler. Transcribed FROM the live scheduler (list_triggers) on 2026-08-21, not from memory. One-shot self-reminders (send_later check-ins) are deliberately out of scope: they are ephemeral work-pacing, not standing lanes.",
 "transcribedFrom": "live scheduler, 2026-09-05 (re-transcribed on moving mac-lane-steward-duty-cycle to hourly and both self-session routines onto lane-deliver; mac-lane-tick added the same day — it is a launchd agent on the owner's Mac, not an account trigger, so it is declared from scripts/mac/install-launchd.sh rather than from list_triggers; earlier: 2026-08-21)",
 "routines": [
  {
   "id": "mac-lane-steward-duty-cycle",
   "trigger": "trig_01XqdtQrmMEtUXTS7pCDr8GS",
   "cron": "24 * * * *",
   "binding": "self-session (resumes the operating session)",
   "authorizedBy": "Dan Fashauer (owner)",
   "authorizedOn": "2026-08-21",
   "authorizationEvidence": "owner message in-session: 'Can you have an employee monitor and administrator for the Mac lane please' — the role was created in direct response, same day. Cadence 4h -> hourly on 2026-09-05, owner message: 'Need you to have a better system with local Mac lane cause this is not working and causing delay'",
   "writeScope": "artifacts/lane-messages/** and artifacts/agent-heartbeats/mac-lane-steward-duty-cycle.json, delivered through scripts/lane-deliver.mjs onto a lane/cloud-mail-* branch that is opened as a PR and auto-merged — NEVER through the code branch (that is what held acks behind open code PRs). Plus: opens a draft PR for every unmerged origin/mac/* branch, subscribes to it, reviews and lands it. Since 2026-09-06 it also carries the duties absorbed from the retired live-sync-loop-keeper: checks pending sim-requests (scripts/check-sim-requests.mjs), unread lane messages (scripts/check-lane-messages.mjs), and the age of artifacts/live-evidence/mac-run.json — when that file is >3 days stale it mails the Mac lane through lane:deliver.",
   "escalationBoundary": "escalates to the owner ONLY what only the owner can clear (credentials, Mac-side actions); a quiet cycle produces no owner message. Live-evidence staleness (artifacts/live-evidence/mac-run.json older than 3 days) goes to the Mac lane as lane mail, never to the owner.",
   "heartbeatPath": "artifacts/agent-heartbeats/mac-lane-steward-duty-cycle.json",
   "cadenceToleranceHours": 3
  },
  {
   "id": "live-sync-loop-keeper",
   "trigger": "trig_01SrTcPxYQrZNs43Hq2afwHE",
   "cron": "0 13 * * *",
   "binding": "fresh session per fire",
   "authorizedBy": "Dan Fashauer (owner)",
   "authorizedOn": "2026-07-27",
   "authorizationEvidence": "created from the owner's account during the owner-directed live-sync-loop build (docs/LIVE_SYNC_LOOP.md); creation date is the trigger's created_at",
   "writeScope": "artifacts/sync/live-sync-manifest.json, artifacts/sync/last-evidence-notice.json, and artifacts/agent-heartbeats/live-sync-loop-keeper.json only; never touches lib/ or proofs",
   "escalationBoundary": "notifies the owner only when live evidence is stale >3 days and not recently notified; flags rather than fixes anything large",
   "heartbeatPath": "artifacts/agent-heartbeats/live-sync-loop-keeper.json",
   "cadenceToleranceHours": 50,
   "status": "retired",
   "retiredAt": "2026-09-06T10:10Z",
   "retiredBy": "cloud lane (Batch Y)",
   "retiredReason": "The account trigger fires daily (last run 2026-09-05T13:07Z, status SUCCEEDED, fresh session each fire) and has delivered nothing in 41 days: a fresh session cannot push to SignalGrid_Alpha (direct push is denied), so every fire since 2026-07-27 ended without a heartbeat, a manifest or an evidence notice reaching the tree. Zero deliveries is not a quiet routine; it is a routine that never had a write path. Its duties are absorbed by mac-lane-steward-duty-cycle: pending sim-requests, unread lane messages, and artifacts/live-evidence/mac-run.json staleness (>3 days) -> Mac-lane mail, never an owner message.",
   "retiredEvidence": {
    "command": "git log --all --oneline -- artifacts/agent-heartbeats/live-sync-loop-keeper.json | wc -l",
    "result": "0 on 2026-09-06 — no commit in the repository's whole history has ever touched the declared heartbeat path; git ls-files artifacts/sync/last-evidence-notice.json is likewise empty",
    "triggerLastRun": "2026-09-05T13:07Z, SUCCEEDED, per the account scheduler (list_triggers), which this tree cannot read"
   }
  },
  {
   "id": "nightly-build-agent",
   "trigger": "trig_01WaoZcckmBjv5Pqs8hGNhFS",
   "cron": "0 14 * * *",
   "binding": "fresh session per fire",
   "authorizedBy": "Dan Fashauer (owner)",
   "authorizedOn": "2026-07-15",
   "authorizationEvidence": "created from the owner's account when the nightly backlog agent was commissioned; creation date is the trigger's created_at",
   "writeScope": "claude/build-agent-* branches ONLY — one bounded backlog item per run, pushed for the owner to review; never merges, never writes the mainline",
   "escalationBoundary": "anything ambiguous or refactor-sized: push nothing, leave the item; reports the compare link and stops",
   "heartbeatPath": null,
   "heartbeatPathReason": "its write scope is claude/build-agent-* branches only — a mainline heartbeat commit would WIDEN that scope, which is worse than the gap it fills. Firing evidence is the pushed branch (or the run's completion notification when no item was actionable). A declared null-with-reason is coherent; a silent absence is not.",
   "cadenceToleranceHours": null,
   "status": "retired",
   "retiredAt": "2026-09-02T21:47Z",
   "retiredBy": "the cloud lane, after telling the owner in the 2026-09-02 status update that it would be retired unless he said to keep it; not yet confirmed by him, and reversible on the account in one switch",
   "retiredReason": "designed before this lane could open pull requests itself (it pushed a branch and asked the owner to open the PR). Per the account scheduler's run record, which this tree cannot read: it fired 2026-09-02 at 14:18 UTC, ran about three minutes, and its prompt still carried a stale attribution trailer. What the tree can show: origin carries no claude/build-agent-* branch. The cloud lane does the same job with a review and a merge path, so the trigger was disabled (update_trigger enabled=false).",
   "retiredEvidence": {
    "branchGlob": "claude/build-agent-*",
    "reason": "its write scope was claude/build-agent-* branches only, so a fire after retirement can show nowhere else"
   }
  },
  {
   "id": "cloud-lane-hygiene-sweep",
   "trigger": "trig_01V3PJFfpNNBdxddnqJg2SAj",
   "cron": "40 9 * * *",
   "binding": "self-session (resumes the operating session)",
   "authorizedBy": "Dan Fashauer (owner)",
   "authorizedOn": "2026-08-21",
   "authorizationEvidence": "owner message in-session: 'clean up anything you need for space and also make this a routine check' — created in direct response, same day; the docker-cache removal it performs was explicitly authorized in the same message",
   "writeScope": "the session scratchpad, dead container/package caches on the cloud machine (never the repo tree), and artifacts/agent-heartbeats/cloud-lane-hygiene-sweep.json — the heartbeat delivered through scripts/lane-deliver.mjs onto a lane/cloud-mail-* branch, never the code branch (since 2026-09-05)",
   "escalationBoundary": "escalates to the owner ONLY if free space stays under 1GB after every cleanup step (environment-level fix); a quiet sweep produces no owner message",
   "heartbeatPath": "artifacts/agent-heartbeats/cloud-lane-hygiene-sweep.json",
   "cadenceToleranceHours": 50
  },
  {
   "id": "mac-lane-tick",
   "trigger": "launchd:com.signalgrid.lane-tick (installed by scripts/mac/install-launchd.sh on the owner's Mac; not an account trigger)",
   "cron": "*/30 * * * *",
   "binding": "launchd user agent on the Mac lane's machine, running scripts/mac/lane-tick.sh with no Claude session",
   "authorizedBy": "Dan Fashauer (owner)",
   "authorizedOn": "2026-09-05",
   "authorizationEvidence": "owner message in-session, second time: 'Need you to have a better system with local Mac lane cause this isn't working and causing delay and issues please figure out what is needed and implement it' — the tick removes the human-started session from the path of every Mac operation the cloud queues.",
   "writeScope": "artifacts/agent-heartbeats/mac-lane-tick.json (via lane:deliver, straight to SignalGrid_Alpha) and artifacts/sim-results/** + artifacts/live-evidence/** on mac/tick-<stamp> branches only; never the code branches, never lane mail (only a person acks a message)",
   "escalationBoundary": "runs only what a sim request names from scripts/lib/sim-operations.mjs; refuses a dirty or non-Alpha checkout and says so in its heartbeat; the cloud steward escalates to the owner when this heartbeat is older than its tolerance (the tick is not installed, the Mac is asleep, or origin is unreachable)",
   "heartbeatPath": "artifacts/agent-heartbeats/mac-lane-tick.json",
   "cadenceToleranceHours": 3,
   "status": "awaiting-activation",
   "awaitingReason": "launchd agent not yet installed on the owner's Mac (bash scripts/mac/install-launchd.sh); owner escalated 2026-09-06T04:26Z",
   "awaitingSince": "2026-09-05"
  }
 ]
}
