# Microsoft Graph Sandbox Fixtures

These fixtures are fake, deterministic, and public-safe. They model Microsoft Graph, Entra, and Intune-style read-only signals without secrets, tenant IDs, customer data, PHI, PII, tokens, live Graph URLs, or production authentication.

Use these fixtures to validate the read-only signal model before any later PC-only live sandbox smoke test.
