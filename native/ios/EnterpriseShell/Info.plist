<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleDevelopmentRegion</key>
    <string>$(DEVELOPMENT_LANGUAGE)</string>
    <key>CFBundleDisplayName</key>
    <string>Enterprise Shell</string>
    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>
    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>
    <key>CFBundleName</key>
    <string>$(PRODUCT_NAME)</string>
    <key>CFBundlePackageType</key>
    <string>APPL</string>
    <key>CFBundleShortVersionString</key>
    <string>1.0</string>
    <key>CFBundleVersion</key>
    <string>1</string>
    <key>LSRequiresIPhoneOS</key>
    <true/>
    <!-- Kiosk: force full screen. Without this, iPadOS lets Split View / Slide Over /
         Stage Manager put the shell in a resizable floating window — which both looks
         broken and lets a user shove the "kiosk" aside. A kiosk must always own the
         whole screen and never be windowed or multitasked. -->
    <key>UIRequiresFullScreen</key>
    <true/>
    <key>UIApplicationSceneManifest</key>
    <dict>
        <key>UIApplicationSupportsMultipleScenes</key>
        <false/>
        <key>UISceneConfigurations</key>
        <dict>
            <key>UIWindowSceneSessionRoleApplication</key>
            <array>
                <dict>
                    <key>UISceneConfigurationName</key>
                    <string>Default Configuration</string>
                    <key>UISceneDelegateClassName</key>
                    <string>$(PRODUCT_MODULE_NAME).SceneDelegate</string>
                </dict>
            </array>
        </dict>
    </dict>
    <key>UILaunchScreen</key>
    <dict>
        <key>UIColorName</key>
        <string>LaunchBackground</string>
    </dict>
    <key>UIRequiredDeviceCapabilities</key>
    <array>
        <string>armv7</string>
    </array>
    <key>UISupportedInterfaceOrientations~ipad</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationPortraitUpsideDown</string>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>
    <key>UIBackgroundModes</key>
    <array>
        <string>fetch</string>
        <string>processing</string>
    </array>
    <key>NSExternalAccessoryManualSelect</key>
    <true/>
    <key>NSFaceIDUsageDescription</key>
    <string>Verify it's you before performing a sensitive action on this shared device.</string>
    <!-- App Transport Security: ATS stays fully enforced for internet endpoints — a
         production SignalGrid control plane MUST be HTTPS. NSAllowsLocalNetworking
         permits cleartext ONLY to local/dev hosts (localhost, *.local, link-local),
         so the embedded gate's dev/demo control plane (-DemoBackendURL http://…)
         resolves on a real device without weakening security for real traffic. -->
    <key>NSAppTransportSecurity</key>
    <dict>
        <key>NSAllowsLocalNetworking</key>
        <true/>
    </dict>
    <!-- Required so EASession(accessory:forProtocol:) can open USB-C/Lightning badge
         readers; without it EASession init returns nil and readers never connect. -->
    <key>UISupportedExternalAccessoryProtocols</key>
    <array>
        <string>com.enterprise.badgereader</string>
    </array>
    <!-- Required on iOS 13+: the OS terminates the app on first CBCentralManager use
         (the selectable BLE badge-reader provider) if this key is absent. -->
    <key>NSBluetoothAlwaysUsageDescription</key>
    <string>Enterprise Shell connects to a Bluetooth badge reader to authenticate users on this shared device.</string>
</dict>
</plist>
