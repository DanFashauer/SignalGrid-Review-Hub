{
  "_comment": "Fixture-backed drafts for the build-lane model tap (DR-044). The public Review Hub makes NO live API calls (AGENTS.md): the real free/local gateway client (DR-029) lives OUT OF THIS REPO and is referenced only. In-repo, draftWithModel() returns one of these canned drafts for a routable task class, or null. Every draft is provenance.verified === false and must be re-checked by a gate or the coordinating Claude session before use.",
  "dev-summary": "Draft dev summary (fixture): the change touches the named files; re-derive every figure by command and re-read each edit site before trusting this line.",
  "log-triage": "Draft triage (fixture): group the log lines by the first error token; treat this grouping as a candidate only, confirmed by re-running the failing command.",
  "ci-triage": "Draft CI triage (fixture): the earliest non-success step is the candidate root cause; read its job log before acting."
}
